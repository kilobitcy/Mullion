# Rust
/target

# 编辑器 / OS
*.swp
.DS_Store

# 交叉编译产物与发版草稿:体积大、每版都变,只走 GitHub Release 不入库
/mullion.exe
/mullion.exe.sha256
/notes.md

# 本地待办与工具落盘,跟代码无关
/todo.md
/.playwright-mcp/

# 本地权限缓存,每台机器不同、只增不减;`.claude/skills/` 反而必须入库
# (发版流程的权威细节都在里面,CLAUDE.md 只留骨架 + 指针)
/.claude/settings.local.json
