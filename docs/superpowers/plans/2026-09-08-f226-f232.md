# 切片 F226~F232 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一次性交付七件事——远端栏就地新建文件夹、远端栏内拖拽移动、拨测结果不再被旧错误压住、会话克隆、跨进程 SFTP 复制粘贴、本地栏默认目录回退到收藏首条、分屏预设高亮改成从树派生——然后发一版 Windows exe 给实机验收。

**Architecture:** 七个特性彼此独立，按「风险从低到高」串成七个 Part：F228(改一处状态机) → F232(删一个字段、加一个纯函数) → F231(纯函数 + 一处接线) → F226(复用 F219 的幽灵行) → F229(store 加一个方法 + 菜单项) → F227(把栏内拖拽路由进 F220 的粘贴流水线) → F230(Windows 系统剪贴板双格式 + 指纹快路径)。每个 Part 自带测试与提交，做完一个提交一个，中途叫停也不留半截。

**Tech Stack:** Rust workspace（`mullion-core` / `mullion-term` / `mullion-ssh` / `mullion-store` / `mullion-app`）、egui 0.30、winit 0.30、wgpu、russh、windows-rs 0.59（OLE/COM 剪贴板）。

---

## 术语与硬约束（动手前必读）

- **依赖方向**：`app → {core, term, ssh, store}`，其余互不依赖。本计划**不**新增跨 crate 依赖。
- **F 编号**：F226 就地新建文件夹 / F227 远端栏内拖拽 / F228 拨测卡片让位 / F229 会话克隆 / F230 跨进程剪贴板 / F231 本地默认目录 / F232 预设高亮派生。提交摘要与测试名都要带编号。
- **测试纪律**（`~/.claude/projects/-data-Mullion/memory/subagent-driven-review-lessons.md`）：每条守护测试的 doc comment 里必须写「自证会变红：把 X 改成 Y」，并且**真的做一次变异验证**。变异验证前**必须先 `git commit`**——本仓库已有 5 次未提交编辑被 `git checkout` 吞掉的记录。
- **等价变异不算自证**：改完仍语义相同的变异（例如把 `!x.is_empty()` 换成 `x.len() > 0`）不构成证明。
- **「绿」的定义**：`cargo test --workspace` 全过 **且** `cargo clippy --workspace --all-targets -- -D warnings` 无输出。
- **T9 字形白名单**：任何新增 UI 字符串里的非 ASCII 符号，要么在 GBK 内，要么登记进 `ui::glyphs::VERIFIED`，否则 `tests/glyph_whitelist.rs` 会红。本计划新增的中文全在 GBK 内，但**不要**顺手用 `…`/`→`/`✓` 之类符号，除非先查白名单。
- **明文凭据**：`SecretEntry`、`EditorBuffer` **不 derive Debug**。F229 深拷贝凭据时不许把它们 `{:?}` 进日志。
- **大输出落盘再 grep**：`cargo test --workspace > /tmp/test.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/test.log`。

---

## File Structure

| 文件 | 归属 | 职责 |
|---|---|---|
| `crates/mullion-app/src/ui/mod.rs` | F228 | 新增 `UiState::begin_probe`，把「发起拨测」收成唯一入口 |
| `crates/mullion-app/src/ui/session_manager/editor.rs` | F228 | 测试连接按钮改调 `begin_probe` |
| `crates/mullion-app/src/shell/workspace/preset.rs` | F232 | 新增纯函数 `preset_of(&Node) -> Option<Preset>` + `same_shape` |
| `crates/mullion-app/src/app.rs` | F232/F226/F227/F229/F230 | 删 `current_preset` 字段；快捷键；粘贴流水线复用；克隆接线；剪贴板事件 |
| `crates/mullion-app/src/files/local.rs` | F231 | 拆出纯函数 `pick_default_local` |
| `crates/mullion-app/src/files/state.rs` | F226 | `NewEdit` 加 `kind: NewKind` |
| `crates/mullion-app/src/ui/files_panel.rs` | F226/F227 | 幽灵行支持文件夹；栏内拖拽落点 |
| `crates/mullion-app/src/ui/files_dialog.rs` | F226 | 删 `FilesDialog::NewDir` 模态 |
| `crates/mullion-app/src/files/drag.rs` | F227 | 放行 remote→remote，落点带移动/复制意图 |
| `crates/mullion-app/src/files/clip.rs` | F227 | 复用既有的 `RemoteClip` / `ClipMode` / 粘贴计划，**不改** |
| `crates/mullion-app/src/lib.rs` | F230 | 挂上 `pub mod clipboard_remote;` |
| `crates/mullion-store/src/vault.rs` | F229 | 新增 `clone_session` |
| `crates/mullion-app/src/ui/session_manager/list.rs` | F229 | 行右键菜单加「克隆」 |
| `crates/mullion-app/src/dragout/win.rs` | F230 | 常驻 STA 线程 + `OleSetClipboard` |
| `crates/mullion-app/src/dragout/descriptor.rs` | F230 | 描述符带相对路径（支持目录展开） |
| `crates/mullion-app/src/clipboard_remote.rs`（新建） | F230 | Mullion 私有剪贴板格式的纯编解码 + 指纹比对，零 Windows API |

---

## Part 1 — F228：点「测试连接」时让旧错误卡片让位

**问题**：`editor.rs` 的拨测结果卡片被 `let error_shown = ui_state.last_error.is_some() && !ui_state.error_dismissed;` 整张压住。上一次保存/拨测失败留下的 `last_error` 是**全局粘性**的，用户再点「测试连接」时，连「正在测试连接…」都不画——按钮看起来像坏了。

**决定**（已与用户确认）：在**点按钮那一刻**把旧错误折叠（`error_dismissed = true`，**内容不清空**），而不是等结果回来才折。不提供「重新展开旧错误」的入口。

### Task 1.1：把「发起拨测」收成 `UiState::begin_probe`

**Files:**
- Modify: `crates/mullion-app/src/ui/mod.rs`（`impl UiState`，紧接 `set_error` 之后）
- Test: 同文件 `mod tests`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/ui/mod.rs` 的 `mod tests` 里（放在 `close_session_manager_clears_probe_form_...` 附近）：

```rust
    /// F228:拨测结果卡片与 `last_error` 卡片互斥,而 `last_error` 是**粘性**的
    /// (只有用户点 × 才 dismiss)。上一次失败留下的错误会把整张拨测卡片压住 ——
    /// 连"正在测试连接…"都不画,按钮看起来像坏了。发起拨测这一刻必须让旧错误
    /// 让位:`error_dismissed = true`,但**内容不清**(它可能是保存失败,还得留着)。
    ///
    /// 自证会变红:把 `begin_probe` 里的 `self.error_dismissed = true;` 删掉,
    /// 第二条断言立刻报「旧错误仍展开着」。
    #[test]
    fn starting_a_probe_folds_away_the_stale_error_so_the_verdict_is_visible() {
        let mut st = UiState::default();
        st.set_error("保存失败:权限不足".to_owned());
        assert!(!st.error_dismissed, "前提:刚报的错是展开着的");

        st.begin_probe(session_manager::EditorBuffer::default());

        assert!(st.probe_click, "必须把点击意图传下去");
        assert!(st.probe_form.is_some(), "必须冻结当时的表单副本");
        assert!(
            st.error_dismissed,
            "点「测试连接」必须让旧错误卡片让位,否则拨测卡片整张不画"
        );
        assert_eq!(
            st.last_error.as_deref(),
            Some("保存失败:权限不足"),
            "只是收起来,内容不许清 —— 那条错误可能还没被处理"
        );
    }
```

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib ui::tests::starting_a_probe_folds_away 2>&1 | tail -20
```
预期：编译失败，`no method named 'begin_probe' found for struct 'UiState'`。

- [ ] **Step 3：实现**

在 `crates/mullion-app/src/ui/mod.rs` 的 `impl UiState` 中，紧跟在 `set_error` 之后插入：

```rust
    /// F228:发起一次拨测。**所有**发起点都必须走这里,不要直接赋值
    /// `probe_click` —— 这里顺带把粘性的 `last_error` 卡片收起来。
    ///
    /// 为什么在**点击**时收而不是在结果回来时收:两张卡片互斥且 `last_error`
    /// 优先(见 `editor.rs` 的 `error_shown`),不收的话连"正在测试连接…"都
    /// 不画,用户看到的是一个点了毫无反应的按钮 —— 20 秒后才突然出结果。
    ///
    /// 内容**不清**:那条错误多半是刚才保存失败,还需要用户处理,清掉等于
    /// 把一条待办悄悄吞了。这里只把它折起来给结果让位。
    pub fn begin_probe(&mut self, form: session_manager::EditorBuffer) {
        self.probe_click = true;
        self.probe_form = Some(form);
        self.error_dismissed = true;
    }
```

- [ ] **Step 4：跑测试确认它绿**

```bash
cargo test -p mullion-app --lib ui::tests::starting_a_probe_folds_away 2>&1 | tail -5
```
预期：`test result: ok. 1 passed`。

- [ ] **Step 5：改按钮调用点**

`crates/mullion-app/src/ui/session_manager/editor.rs`，把测试连接按钮体内的两行替换掉：

```rust
        if super::labeled_button(
            ui,
            super::probe_button_id(),
            "测试连接",
            !disable_connect,
            probe_tip.as_deref(),
        ) {
            // F228:走 `begin_probe`,它顺带把粘性的旧错误卡片折起来 ——
            // 否则拨测卡片(含"正在测试连接…")被 `error_shown` 整张压住。
            ui_state.begin_probe(buf.clone());
        }
```

（原来是 `ui_state.probe_click = true; ui_state.probe_form = Some(buf.clone());`。）

- [ ] **Step 6：加一条源码切片守护，防止有人绕回去直接赋值**

加到 `crates/mullion-app/src/ui/mod.rs` 的 `mod tests`：

```rust
    /// F228:`probe_click = true` 只许出现在 `begin_probe` 里。绕过它直接赋值的
    /// 调用点不会折叠旧错误,于是 bug 原样复发,而且**画面上看不出来** ——
    /// 只有在「上一次报过错」这个前提下才复现。
    ///
    /// 判据用裸赋值串而不是函数名:检查的就是"有没有人手写这一句"。
    ///
    /// 自证会变红:把上面 `begin_probe` 的实现搬回 `editor.rs` 的按钮体内。
    #[test]
    fn probe_click_is_only_ever_set_inside_begin_probe() {
        let src = include_str!("session_manager/editor.rs");
        assert!(
            !src.contains("probe_click = true"),
            "editor.rs 不许直接置 probe_click,必须走 UiState::begin_probe(F228)"
        );
        assert!(
            src.contains("begin_probe("),
            "editor.rs 的测试连接按钮必须调 begin_probe"
        );
    }
```

- [ ] **Step 7：跑绿 + clippy**

```bash
cargo test -p mullion-app > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
cargo clippy -p mullion-app --all-targets -- -D warnings 2>&1 | tail -5
```
预期：`test result: ok`，clippy 无输出。

- [ ] **Step 8：变异验证**

先提交（见 Step 9），再依次做两次变异并确认对应测试变红，然后 `git checkout` 撤回：
1. 删 `begin_probe` 里的 `self.error_dismissed = true;` → `starting_a_probe_folds_away...` 红。
2. 把 `editor.rs` 里的 `ui_state.begin_probe(buf.clone());` 换回 `ui_state.probe_click = true; ui_state.probe_form = Some(buf.clone());` → `probe_click_is_only_ever_set_inside_begin_probe` 红。

- [ ] **Step 9：提交**

```bash
git add crates/mullion-app/src/ui/mod.rs crates/mullion-app/src/ui/session_manager/editor.rs
git commit -m "fix(app): 点「测试连接」时折叠粘性旧错误,拨测卡片不再整张被压住 (F228)

旧错误卡片与拨测卡片互斥且前者优先,上一次保存失败留下的 last_error
会让「正在测试连接…」都画不出来,按钮看起来像坏了。发起点收成
UiState::begin_probe,在点击那一刻 error_dismissed = true(内容不清)。

守护:ui::tests::starting_a_probe_folds_away_the_stale_error_so_the_verdict_is_visible
      ui::tests::probe_click_is_only_ever_set_inside_begin_probe

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Part 2 — F232：分屏预设高亮改成从布局树派生

**问题**：`TerminalTab::current_preset: Option<Preset>` 是一份**影子状态**。`apply_layout_actions` 在关掉任一 pane 后无条件 `preset_out = None`——于是 2×2 关掉一块后，工具栏所有按钮都不高亮，即使剩下的形状正好等于「两屏左右」。

**决定**（已与用户确认，方案 A）：**删掉字段**，改成每帧从 `ws.tree()` 现算 `preset_of(tree)`。

**已知并接受的副作用**：
- 拖动分隔条改了比例后，高亮会熄灭（形状确实不再等于任何预设）。
- `app.rs:7851` 的恢复启发式（`(p.tree.len() == 1).then_some(Preset::Single)`）一并删除——`preset_of` 会自己认出单叶子就是 `Single`。

**已核实的真实行为**（`preset_tree` 原文推出，写进测试）：
- 任一两屏预设关掉一块 → `Single`。
- `ThreeBigLeft` 关掉通高那块 → `TwoTopBottom`；关掉小块之一 → `TwoLeftRight`。
- `ThreeColumns` 关掉中间那块 → **`None`**（外层 ratio 仍是 1/3，不等于 `TwoLeftRight` 的 0.5）。
- `FourGrid` 关掉任意一块 → **`None`**（外层是竖分，剩下的形状不匹配任何预设）。

### Task 2.1：纯函数 `preset_of`

**Files:**
- Modify: `crates/mullion-app/src/shell/workspace/preset.rs`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/shell/workspace/preset.rs` 的 `mod tests`：

```rust
    /// F232:每个预设自己的树必须被认回自己。这条是 `preset_of` 的自反性 ——
    /// 认不回来的话工具栏点完预设当场就不高亮。
    #[test]
    fn every_preset_tree_is_recognised_as_itself() {
        for p in Preset::ALL {
            assert_eq!(
                preset_of(&preset_tree(p, &ids(p.pane_count() as u32))),
                Some(p),
                "{p:?} 认不回自己"
            );
        }
    }

    /// F232:`preset_of` 只看**形状**,不看 pane id —— 树上的 id 是运行期分配的,
    /// 跟预设无关。
    ///
    /// 自证会变红:把 `same_shape` 的 `(Node::Leaf(_), Node::Leaf(_)) => true`
    /// 改成 `(Node::Leaf(a), Node::Leaf(b)) => a == b`,本条红。
    #[test]
    fn preset_of_ignores_pane_ids() {
        let tree = preset_tree(Preset::TwoLeftRight, &[PaneId(77), PaneId(9)]);
        assert_eq!(preset_of(&tree), Some(Preset::TwoLeftRight));
    }

    /// F232 的全部意义:关掉一块 pane 之后,如果剩下的形状**正好**等于某个预设,
    /// 工具栏那个按钮就该重新亮起来。旧实现在这里无条件把高亮清成 None。
    ///
    /// 自证会变红:让 `preset_of` 恒返回 `None`,四条 assert_eq 全红。
    #[test]
    fn closing_a_pane_can_relight_a_preset_button() {
        use mullion_core::layout::close_pane;

        // 两屏左右,关掉右边 → 单屏。
        let mut t = preset_tree(Preset::TwoLeftRight, &ids(2));
        assert!(close_pane(&mut t, PaneId(2)));
        assert_eq!(preset_of(&t), Some(Preset::Single));

        // 两屏上下,关掉下面 → 单屏。
        let mut t = preset_tree(Preset::TwoTopBottom, &ids(2));
        assert!(close_pane(&mut t, PaneId(2)));
        assert_eq!(preset_of(&t), Some(Preset::Single));

        // 左满高三屏,关掉通高的那块 → 右侧上下两块顶替 → 两屏上下。
        let mut t = preset_tree(Preset::ThreeBigLeft, &ids(3));
        assert!(close_pane(&mut t, PaneId(1)));
        assert_eq!(preset_of(&t), Some(Preset::TwoTopBottom));

        // 左满高三屏,关掉右上那块 → 左通高 + 右一块 → 两屏左右。
        let mut t = preset_tree(Preset::ThreeBigLeft, &ids(3));
        assert!(close_pane(&mut t, PaneId(2)));
        assert_eq!(preset_of(&t), Some(Preset::TwoLeftRight));
    }

    /// F232 的另一半:剩下的形状**不**等于任何预设时必须老实返回 `None`,
    /// 不许"就近凑一个"。凑错了会让用户以为当前是某个预设,再点一次同名按钮
    /// 反而重排整棵树、真的关掉 pane。
    ///
    /// 这两例是从 `preset_tree` 原文推出来的,不是猜的:
    /// - `ThreeColumns` = `split(h, 1/3, l0, split(h, 0.5, l1, l2))`,关掉中间
    ///   那块后外层 ratio 仍是 1/3,不等于 `TwoLeftRight` 的 0.5。
    /// - `FourGrid` 外层是**竖**分(上下两行),关掉右下后剩「上行左右分 + 下行
    ///   通宽」,横竖方向对不上任何三屏预设。
    ///
    /// 自证会变红:把 `RATIO_EPS` 从 1e-3 放大到 0.2,第一条红。
    #[test]
    fn a_shape_that_matches_no_preset_reports_none() {
        use mullion_core::layout::close_pane;

        let mut t = preset_tree(Preset::ThreeColumns, &ids(3));
        assert!(close_pane(&mut t, PaneId(2)));
        assert_eq!(preset_of(&t), None, "1/3 : 2/3 不是任何预设");

        let mut t = preset_tree(Preset::FourGrid, &ids(4));
        assert!(close_pane(&mut t, PaneId(4)));
        assert_eq!(preset_of(&t), None, "上行左右分 + 下行通宽,不是任何预设");
    }

    /// F232:用户拖了分隔条 → 比例不再是预设值 → 高亮熄灭。这是**有意**的:
    /// 高亮的含义是"当前形状就是这个预设",不是"上次点了这个按钮"。
    #[test]
    fn dragging_a_splitter_extinguishes_the_highlight() {
        let mut t = preset_tree(Preset::TwoLeftRight, &ids(2));
        if let Node::Split { ratio, .. } = &mut t {
            *ratio = 0.7;
        } else {
            panic!("两屏预设的根必须是 Split");
        }
        assert_eq!(preset_of(&t), None);
    }
```

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib shell::workspace::preset 2>&1 | tail -20
```
预期：编译失败，`cannot find function 'preset_of'`。

- [ ] **Step 3：实现**

在 `crates/mullion-app/src/shell/workspace/preset.rs` 中，`preset_tree` 之后插入：

```rust
/// 比例相等的判定阈值。预设里出现的比例只有 0.5 和 1/3,两者相差 1/6,
/// 1e-3 远小于它 —— 既认得出 `1.0 / 3.0` 的浮点表示,又不会把用户拖出来的
/// 0.49 误判成 0.5(拖动的最小可见增量远大于 1e-3)。
const RATIO_EPS: f32 = 1e-3;

/// 当前这棵树的形状是否**正好**等于某个预设。是则返回它,用于工具栏高亮(F232)。
///
/// 这是个**派生量**,不是被保存的状态:曾经有一个 `TerminalTab::current_preset`
/// 字段影子跟踪它,而"关掉一个 pane 就无条件清空高亮"是那份影子状态唯一的
/// 更新规则 —— 于是「两屏关掉一块后其实就是单屏」这种情况下,单屏按钮不亮。
/// 从树现算就没有这类漂移:形状是什么就是什么。
///
/// 代价是拖动分隔条会熄灭高亮(比例不再是预设值)。这是对的:高亮的含义是
/// "当前形状就是这个预设",不是"上次点了哪个按钮"。
pub fn preset_of(tree: &Node) -> Option<Preset> {
    Preset::ALL.into_iter().find(|p| {
        let ids: Vec<PaneId> = (1..=p.pane_count() as u32).map(PaneId).collect();
        same_shape(tree, &preset_tree(*p, &ids))
    })
}

/// 两棵树形状是否一致:方向与比例逐层比,**叶子上的 `PaneId` 一律不看** ——
/// id 是运行期分配的,跟"这是哪个预设"无关。
fn same_shape(a: &Node, b: &Node) -> bool {
    match (a, b) {
        (Node::Leaf(_), Node::Leaf(_)) => true,
        (
            Node::Split {
                dir: d1,
                ratio: r1,
                a: a1,
                b: b1,
            },
            Node::Split {
                dir: d2,
                ratio: r2,
                a: a2,
                b: b2,
            },
        ) => {
            d1 == d2 && (r1 - r2).abs() < RATIO_EPS && same_shape(a1, a2) && same_shape(b1, b2)
        }
        _ => false,
    }
}
```

- [ ] **Step 4：跑测试确认它绿**

```bash
cargo test -p mullion-app --lib shell::workspace::preset 2>&1 | tail -5
```
预期：全部 ok。

- [ ] **Step 5：提交**

```bash
git add crates/mullion-app/src/shell/workspace/preset.rs
git commit -m "feat(app): 新增纯函数 preset_of,从布局树认出当前预设 (F232)

守护:preset::tests::closing_a_pane_can_relight_a_preset_button
      preset::tests::a_shape_that_matches_no_preset_reports_none

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 2.2：删掉 `current_preset` 字段，改用派生量

**Files:**
- Modify: `crates/mullion-app/src/app.rs`（字段 732、初值 7795、恢复启发式 7851、读取 10888、写回 11105、`apply_layout_actions` 11991-12019、测试里的 7 处构造与 2 条测试）

- [ ] **Step 1：改 `apply_layout_actions` 的签名与返回值**

把 `crates/mullion-app/src/app.rs:11991` 起的函数改成：

```rust
/// 返回 `None` 表示这一帧没有布局动作,调用方不需要标脏/开新 channel。
/// 返回 `Some(新增待开 channel 的 pane id)`——真正开 channel 需要
/// `_runtime`/`proxy`,留给调用方的 `App::spawn_fresh_panes`。
///
/// F232:不再返回"新的 current_preset"。工具栏高亮改由 `preset_of(ws.tree())`
/// 每帧现算 —— 影子状态唯一的更新规则("关了 pane 就清空")本身就是那个 bug。
fn apply_layout_actions(ws: &mut Workspace, actions: &crate::ui::UiActions) -> Option<Vec<PaneId>> {
    if actions.preset.is_none() && actions.close_pane.is_none() {
        return None;
    }
    let mut fresh = Vec::new();
    let mut changed = false;
    if let Some(preset) = actions.preset {
        fresh = ws.apply_preset(preset);
        changed = true;
    }
    if let Some(id) = actions.close_pane {
        // close_pane 在「只剩最后一个 pane」时会拒绝并返回 false、树不变——
        // 那一帧就是纯粹的 noop,不该标脏。
        if ws.close_pane(id) {
            changed = true;
        }
    }
    changed.then_some(fresh)
}
```

- [ ] **Step 2：改调用点**（`app.rs:11101`）

```rust
                            if let Some(t) = self.active_term_mut() {
                                if let Some(fresh) = apply_layout_actions(&mut t.ws, &actions) {
                                    mark_ui_dirty!(self.ui_dirty);
                                    self.spawn_fresh_panes(fresh);
                                }
                            }
```

- [ ] **Step 3：改读取点**（`app.rs:10888`）

```rust
                                preset: self.active_ws().map(crate::shell::workspace::preset_of).unwrap_or(None),
```

> 若 `preset_of` 未从 `shell::workspace` re-export，改用它在 `preset` 模块下的实际路径；`active_ws()` 返回 `Option<&Workspace>`，`preset_of` 收 `&Node`，所以这一行实际写成
> `preset: self.active_ws().and_then(|ws| crate::shell::workspace::preset::preset_of(ws.tree())),`
> ——以 `Workspace::tree()` 的真实签名为准，编译报错时看提示的实际签名，不要猜。

- [ ] **Step 4：删字段与两处赋值**

- 删 `app.rs:732` 的 `current_preset: Option<Preset>,`（连同其 doc comment）。
- 删 `app.rs:7795` 的 `current_preset: Some(Preset::Single),`。
- 删 `app.rs:7851` 的 `t.current_preset = (p.tree.len() == 1).then_some(Preset::Single);` 及其上方两行注释。
- 删测试构造里的 7 处 `current_preset: None,`（19165 / 19440 / 20292 / 21455 / 21537 / 21619 / 22454，行号会随删改漂移，用 `grep -n current_preset` 复查）。

- [ ] **Step 5：改两条受影响的既有测试**

`app.rs:16443` 的 `closing_the_last_pane_is_a_noop_and_does_not_clear_current_preset` 改名并改断言（它现在守的是「noop 帧不标脏」）：

```rust
    /// `close_pane` 在"只剩最后一个 pane"时会拒绝并返回 false、树不变。这一帧
    /// 必须是纯粹的 noop —— 返回 `Some(..)` 的话调用方会标脏并触发一次无意义
    /// 重绘(T3 红线)。
    ///
    /// F232:原来这条还守着"不许清掉 current_preset";那个字段已经删了,高亮
    /// 由 `preset_of` 现算,树没变高亮自然不会变。
    ///
    /// 自证会变红:把 `apply_layout_actions` 里 `if ws.close_pane(id)` 的条件
    /// 去掉(无条件 `changed = true`),本条红。
    #[test]
    fn closing_the_last_pane_is_a_pure_noop() {
        let mut ws = Workspace::new(test_pane(1), 0);
        let only_id = mullion_core::layout::leaves(ws.tree())[0];
        let result = apply_layout_actions(
            &mut ws,
            &crate::ui::UiActions {
                preset: None,
                close_pane: Some(only_id),
                ..Default::default()
            },
        );
        assert!(result.is_none(), "关最后一个 pane 是纯粹的 noop,不该标脏");
        assert_eq!(mullion_core::layout::leaves(ws.tree()).len(), 1);
    }
```

`app.rs:16389` 的 `pane_still_wanted_rejects_a_leaf_dropped_by_a_later_preset_switch` 里两处解构改成单值：

```rust
        let fresh = apply_layout_actions(/* 原样 */).expect("点了预设,动作不该是 None");
        ...
        let fresh2 = apply_layout_actions(/* 原样 */).expect("点了预设,动作不该是 None");
```

- [ ] **Step 6：跑绿 + clippy**

```bash
cargo test --workspace > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
```

- [ ] **Step 7：确认字段真的没了**

```bash
grep -rn "current_preset" crates/ ; echo "exit=$?"
```
预期：只剩 `preset.rs:14` 那句 doc comment（顺手把它改成「也是刚连上、只有一个 pane 时 `preset_of` 会认出的形状」），其余零命中。

- [ ] **Step 8：提交**

```bash
git add crates/mullion-app/src/app.rs crates/mullion-app/src/shell/workspace/preset.rs
git commit -m "fix(app): 分屏预设高亮改从布局树派生,关掉一块后剩下的形状能重新点亮 (F232)

TerminalTab::current_preset 是影子状态,唯一的更新规则是「关了 pane 就清空」
——那条规则本身就是 bug:两屏关掉一块其实就是单屏,按钮却不亮。字段删掉,
工具栏每帧从 preset_of(ws.tree()) 现算。

代价(有意接受):拖动分隔条改了比例后高亮熄灭;F37 恢复的形状不再靠
「叶子数 == 1」猜成 Single,由 preset_of 自己认。

守护:app::tests::closing_the_last_pane_is_a_pure_noop
      preset::tests::closing_a_pane_can_relight_a_preset_button

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Part 3 — F231：本地栏默认目录回退到「收藏的第一条」

**问题**：本地栏永远开在 `%USERPROFILE%`。用户天天去的是收藏里的第一条目录，每次都得再点一下。

**决定**（已与用户确认）：优先级 `会话配置的默认本地目录` → `Settings::local_bookmarks[0]` → `%USERPROFILE%` → 当前工作目录。收藏首条要做存在性检查；**不存在就直接回退到主目录**，不试第二条（避免「开在一个我完全没预期的地方」）。不加「调整收藏顺序」的 UI——顺序就是数组顺序（F121 已经是这个约定）。

### Task 3.1：把选择逻辑拆成纯函数

**Files:**
- Modify: `crates/mullion-app/src/files/local.rs:158-174`
- Test: 同文件 `mod tests`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/files/local.rs` 的 `mod tests`：

```rust
    /// F231:优先级是「会话配置 > 收藏首条 > 主目录 > cwd」。会话里明确填了
    /// 默认本地目录,收藏就不该越过它 —— 那是用户对**这条会话**的明确表态。
    #[test]
    fn a_configured_default_beats_the_first_bookmark() {
        let d = pick_default_local(
            Some("/srv/incoming"),
            Some("/home/me/proj"),
            true,
            Some(RemotePath::from_bytes(b"/home/me".to_vec())),
            RemotePath::from_bytes(b"/tmp".to_vec()),
        );
        assert_eq!(d.as_bytes(), b"/srv/incoming");
    }

    /// F231 的正事:会话没填 → 开在收藏的第一条。
    ///
    /// 自证会变红:把 `pick_default_local` 里那一段收藏分支删掉,本条落到
    /// 主目录 `/home/me`,断言红。
    #[test]
    fn an_unconfigured_panel_opens_at_the_first_bookmark() {
        let d = pick_default_local(
            None,
            Some("/home/me/proj"),
            true,
            Some(RemotePath::from_bytes(b"/home/me".to_vec())),
            RemotePath::from_bytes(b"/tmp".to_vec()),
        );
        assert_eq!(d.as_bytes(), b"/home/me/proj");
    }

    /// F231:收藏首条指向一个已经不存在的目录(换机器、盘符变了)时,面板必须
    /// 回到主目录 —— 而**不是**去试第二条。试第二条的话,用户看到的是一个
    /// 自己完全没预期的目录,而且没有任何提示解释为什么。
    ///
    /// 自证会变红:把 `pick_default_local` 里的 `&& bookmark_exists` 去掉,
    /// 本条返回 `/mnt/gone`,断言红。
    #[test]
    fn a_missing_first_bookmark_falls_straight_back_to_home() {
        let d = pick_default_local(
            None,
            Some("/mnt/gone"),
            false,
            Some(RemotePath::from_bytes(b"/home/me".to_vec())),
            RemotePath::from_bytes(b"/tmp".to_vec()),
        );
        assert_eq!(d.as_bytes(), b"/home/me");
    }

    /// F231:空白路径的收藏等于没有(同 `configured` 的既有约定) —— 面板不能
    /// 开在一个名叫「   」的目录上。
    #[test]
    fn a_blank_bookmark_path_counts_as_no_bookmark() {
        let d = pick_default_local(
            None,
            Some("   "),
            true,
            Some(RemotePath::from_bytes(b"/home/me".to_vec())),
            RemotePath::from_bytes(b"/tmp".to_vec()),
        );
        assert_eq!(d.as_bytes(), b"/home/me");
    }

    /// F231:主目录都拿不到时(`directories` 在某些精简环境返回 None)落到 cwd。
    /// 任何一步都不 panic —— 面板宁可开在奇怪的地方也不能开不出来。
    #[test]
    fn everything_missing_falls_back_to_the_working_directory() {
        let d = pick_default_local(
            None,
            None,
            false,
            None,
            RemotePath::from_bytes(b"/tmp".to_vec()),
        );
        assert_eq!(d.as_bytes(), b"/tmp");
    }
```

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib files::local 2>&1 | tail -20
```
预期：编译失败，`cannot find function 'pick_default_local'`。

- [ ] **Step 3：实现**

把 `crates/mullion-app/src/files/local.rs:162-174` 整段替换成：

```rust
/// 默认本地目录该开在哪 —— **纯选择逻辑**,零 IO,四个候选全由调用方给进来。
///
/// 拆成纯函数是因为这里唯一容易写错的是**优先级**,而优先级恰恰是唯一
/// 用不着碰文件系统就能验的东西。掺进 `is_dir()` / `home_dir()` 之后,这几条
/// 分支就只能靠"在开发机上跑一下看看开在哪"来确认了。
///
/// `bookmark_exists`:收藏首条指向的目录当下是否真的存在(调用方查)。不存在
/// 时**直接回退主目录**,不试第二条 —— 试下去的话用户会开在一个自己完全没
/// 预期的目录里,而且没有任何提示解释为什么(F231 与用户确认的取舍)。
pub fn pick_default_local(
    configured: Option<&str>,
    bookmark: Option<&str>,
    bookmark_exists: bool,
    home: Option<RemotePath>,
    cwd: RemotePath,
) -> RemotePath {
    // 只有空白等于没填:面板不能开在一个名叫「   」的目录上。
    let usable = |s: &&str| !s.trim().is_empty();
    if let Some(s) = configured.filter(usable) {
        return RemotePath::from_bytes(s.as_bytes().to_vec());
    }
    if let Some(s) = bookmark.filter(usable).filter(|_| bookmark_exists) {
        return RemotePath::from_bytes(s.as_bytes().to_vec());
    }
    home.unwrap_or(cwd)
}

/// [`pick_default_local`] 的带 IO 版本:自己去查主目录、cwd,以及收藏首条
/// 存不存在。`bookmark` 是**本地收藏的第一条**(F231),由调用方从
/// `Settings::local_bookmarks` 取。
pub fn default_local(configured: Option<&str>, bookmark: Option<&str>) -> RemotePath {
    let bookmark_exists = bookmark
        .filter(|s| !s.trim().is_empty())
        .is_some_and(|s| to_path(&RemotePath::from_bytes(s.as_bytes().to_vec())).is_dir());
    let cwd = std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."));
    pick_default_local(
        configured,
        bookmark,
        bookmark_exists,
        home_dir(),
        RemotePath::from_bytes(path_bytes(&cwd)),
    )
}
```

- [ ] **Step 4：改三处既有调用点**

- `local.rs:351` → `let d = default_local(None, None);`
- `local.rs:407` → `let d = default_local(Some("/srv/incoming"), None);`
- `local.rs:410` → `assert_ne!(default_local(Some("   "), None).as_bytes(), b"   ");`
- `ui/files_panel.rs:1831`（`PanelFrame::default`）→ `local: PaneState::new(crate::files::local::default_local(None, None)),`

- [ ] **Step 5：接线到 `PanelFrame::new`**

`crates/mullion-app/src/ui/files_panel.rs:1858` 附近，`PanelFrame::new` 的函数体改成：

```rust
        Self {
            // F231:会话没配默认本地目录时,开在本地收藏的第一条。收藏是**全局**
            // 的(F187 之后存在 settings.toml),顺序即数组顺序(F121)。
            local: PaneState::new(crate::files::local::default_local(
                default_local,
                local_bookmarks.first().map(|b| b.path.as_str()),
            )),
            bookmarks,
            local_bookmarks,
            session_bound,
            ..Self::default()
        }
```

> 注意 `local_bookmarks` 在 `local` 之后才被移动进结构体，所以先借用 `.first()` 是合法的——但字段初始化顺序即求值顺序，`local` 这一行必须排在 `local_bookmarks` **之前**。写反了编译器会报 borrow-after-move。

- [ ] **Step 6：加一条接线守护**

加到 `crates/mullion-app/src/ui/files_panel.rs` 的 `mod tests`：

```rust
    /// F231:`PanelFrame::new` 必须真的把收藏首条喂给 `default_local`。纯函数
    /// 那几条测试守的是"优先级对",守不住"根本没接上" —— 而没接上的表现是
    /// 面板照旧开在主目录,画面上完全看不出是 bug 还是"我没设收藏"。
    ///
    /// 判据是调用形态:`default_local(` 后面必须跟着 `local_bookmarks.first()`。
    ///
    /// 自证会变红:把 `PanelFrame::new` 里第二个实参改回 `None`。
    #[test]
    fn the_new_panel_feeds_the_first_local_bookmark_into_default_local() {
        let src = include_str!("files_panel.rs");
        assert!(
            src.contains("local_bookmarks.first().map(|b| b.path.as_str())"),
            "PanelFrame::new 必须把本地收藏首条喂给 default_local(F231)"
        );
    }
```

- [ ] **Step 7：跑绿 + clippy**

```bash
cargo test -p mullion-app > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
```

- [ ] **Step 8：提交（提交后再做 Step 1/3 两条 doc comment 里写明的变异验证）**

```bash
git add crates/mullion-app/src/files/local.rs crates/mullion-app/src/ui/files_panel.rs
git commit -m "feat(app): 本地栏默认开在收藏的第一条目录 (F231)

优先级:会话配置 > 本地收藏首条 > %USERPROFILE% > cwd。收藏首条不存在时
直接回退主目录,不试第二条 —— 试下去用户会开在完全没预期的地方且无提示。
选择逻辑拆成纯函数 pick_default_local(零 IO),存在性检查留在带 IO 的壳里。

守护:files::local::tests::an_unconfigured_panel_opens_at_the_first_bookmark
      files::local::tests::a_missing_first_bookmark_falls_straight_back_to_home
      ui::files_panel::tests::the_new_panel_feeds_the_first_local_bookmark_into_default_local

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Part 4 — F226：远端栏就地新建文件夹（参照改名的交互）

**问题**：「新建文件夹…」弹一个模态框，而「新建文件」（F219）和「重命名」（F200）都是列表里就地编辑。同一件事两套交互。

**决定**（已与用户确认）：
- 复用 F219 的**幽灵行**（列表首行长出一个输入框）。`NewEdit` 加一个 `kind: NewKind{File, Dir}` 字段。
- 幽灵行给文件夹画**文件夹图标**（新建文件那条维持现状，不画图标）。
- 菜单项文案改成「新建文件夹」，**去掉省略号**——省略号在这套界面里专指「会弹框」。
- `FilesDialog::NewDir` 模态与 `FileAsk::NewDir` 一并删除。
- 快捷键 `Ctrl+Shift+N`，**只在远端栏**（同 `Ctrl+N`）。
- 三个就地输入框（改名 / 新建文件 / 新建文件夹）互斥——后两者共用 `new_edit` 这一个槽，天然互斥，但要有测试钉住。
- 判重按 `entries` 而不是 `rows`（隐藏文件也占名字）。
- 成功后 `OpFollow::Reveal` 把光标落到新目录上，**但不自动进去**。
- 空目录里同样能建。

> `NewEdit` 的文档注释论证的是「不该把改名和新建塞进同一个结构体」（`from` 对新建是假值），**不是**「`NewEdit` 里不能有 kind」。加 `kind` 不违反它——两个 kind 的字段集合完全一样。

### Task 4.1：`NewEdit` 加 `kind`

**Files:**
- Modify: `crates/mullion-app/src/files/state.rs`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/files/state.rs` 的 `mod tests`：

```rust
    /// F226:新建文件夹与新建文件共用同一个槽 —— 它们是**同一种交互的两个
    /// kind**,不是两套状态。共用槽让「互斥」成为类型上的事实,而不是两个
    /// 字段互相记得清对方(F219 已经为 rename/new 各写了一次清对方,再来
    /// 一个字段就是三处两两互清,漏一处就是两个 TextEdit 互抢焦点、
    /// 谁都退不出去)。
    ///
    /// 自证会变红:让 `begin_new_dir` 也写 `NewKind::File`。
    #[test]
    fn new_file_and_new_dir_share_one_slot_and_carry_their_kind() {
        let mut s = PaneState::new(RemotePath::from_bytes(b"/tmp".to_vec()));

        assert!(s.begin_new_file());
        assert_eq!(s.new_edit.as_ref().map(|n| n.kind), Some(NewKind::File));

        assert!(s.begin_new_dir());
        assert_eq!(
            s.new_edit.as_ref().map(|n| n.kind),
            Some(NewKind::Dir),
            "新建文件夹必须顶掉新建文件,而不是并存"
        );
        assert_eq!(s.new_edit.as_ref().map(|n| n.buf.as_str()), Some(""));
    }

    /// F226:新建文件夹同样与改名互斥,两个方向都要成立。
    ///
    /// 自证会变红:把 `begin_new_dir` 里 `self.rename_edit = None;` 删掉
    /// (第一段红);把 `begin_rename` 里 `self.new_edit = None;` 删掉(第二段红)。
    #[test]
    fn new_dir_and_rename_are_mutually_exclusive_both_ways() {
        let mut s = PaneState::new(RemotePath::from_bytes(b"/tmp".to_vec()));
        s.entries = vec![entry("a.txt", EntryKind::File)];
        s.cursor = Some(RemotePath::from_bytes(b"a.txt".to_vec()));

        assert!(s.begin_rename(), "前提:进得了改名态");
        assert!(s.begin_new_dir());
        assert!(s.rename_edit.is_none(), "改名态还赖着 —— 两个输入框会互抢焦点");

        assert!(s.begin_new_dir());
        assert!(s.begin_rename(), "前提:进得了改名态");
        assert!(s.new_edit.is_none(), "新建文件夹态还赖着 —— 同上");
    }
```

> `entry(...)` 辅助函数在该 `mod tests` 里已有（`begin_rename` 那几条测试在用）；沿用它，不要另造一个。若签名不同，以文件里现有的为准。

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib files::state 2>&1 | tail -20
```
预期：`cannot find type 'NewKind'` / `no method named 'begin_new_dir'`。

- [ ] **Step 3：实现**

`crates/mullion-app/src/files/state.rs`，在 `NewEdit` 定义之前插入：

```rust
/// F226:就地新建的是文件还是目录。
///
/// 放进 `NewEdit` 而不是另开一个 `new_dir_edit` 字段:两者的字段集合完全
/// 一样(一个缓冲 + 一个焦点一次性标记),交互也完全一样,唯一的差别是
/// 提交时发 `create_file` 还是 `create_dir`。另开字段的话「互斥」就变成三个
/// 字段两两互清,漏一处就是两个 `TextEdit` 互抢焦点、谁都退不出去。
///
/// (这与 `NewEdit` 文档里"不和 `RenameEdit` 合并"的理由不冲突:那条反对的是
/// 合并**字段集合不同**的两种编辑态 —— 改名多一个 `from`,对新建是假值。)
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NewKind {
    File,
    Dir,
}
```

`NewEdit` 加字段：

```rust
pub struct NewEdit {
    /// 输入框内容。进来时是空的。
    pub buf: String,
    /// 刚进新建态、**还没把键盘焦点要过来**。渲染那侧要一次就清掉
    /// (每帧无条件 `request_focus()` 会让两栏互抢,见 `RenameEdit` 的文档)。
    pub focus_pending: bool,
    /// F226:建的是文件还是目录。
    pub kind: NewKind,
}
```

`begin_new_file` 改成薄壳 + 新增 `begin_new_dir`：

```rust
    /// F219:开始在**当前目录**里就地新建一个文件。返回是否真的进了新建态。
    ///
    /// 不像 `begin_rename` 那样需要光标行 —— 新建不针对任何一条已有的行,
    /// 空目录里同样成立(那正是用户最需要它的时候)。
    pub fn begin_new_file(&mut self) -> bool {
        self.begin_new(NewKind::File)
    }

    /// F226:同 [`Self::begin_new_file`],建的是目录。
    pub fn begin_new_dir(&mut self) -> bool {
        self.begin_new(NewKind::Dir)
    }

    fn begin_new(&mut self, kind: NewKind) -> bool {
        // 互斥,见 `new_edit` 的文档。
        self.rename_edit = None;
        self.new_edit = Some(NewEdit {
            buf: String::new(),
            focus_pending: true,
            kind,
        });
        true
    }
```

- [ ] **Step 4：跑测试确认它绿**

```bash
cargo test -p mullion-app --lib files::state 2>&1 | tail -5
```

- [ ] **Step 5：提交**

```bash
git add crates/mullion-app/src/files/state.rs
git commit -m "refactor(app): NewEdit 带上 kind,新建文件/文件夹共用一个槽 (F226)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 4.2：菜单项与动作改成就地新建

**Files:**
- Modify: `crates/mullion-app/src/ui/files_panel.rs`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/ui/files_panel.rs` 的 `mod tests`：

```rust
    /// F226:「新建文件夹」改成就地编辑,所以**不带省略号** —— 省略号在这套
    /// 界面里专指「会弹个框」(「删除…」「属性(权限)…」还带着)。带着它
    /// 而实际不弹框,用户会以为点错了。
    ///
    /// 自证会变红:把标签改回 `"新建文件夹…"`。
    #[test]
    fn new_dir_is_an_inline_edit_so_it_carries_no_ellipsis() {
        let remote = menu_items_for(PanelColumn::Remote, None, false);
        let e = remote
            .iter()
            .find(|e| e.item == MenuItem::NewDir)
            .expect("远端栏没有「新建文件夹」");
        assert_eq!(e.label, "新建文件夹");
        assert!(
            !e.label.contains('…'),
            "「新建文件夹」带了省略号 —— 那是弹框的记号"
        );
        assert_eq!(
            MenuItem::NewDir.into_action_for_test(),
            FileAction::BeginNewDir,
            "菜单项必须发就地新建,不能再发 Ask(弹框)"
        );
        // 「新建文件夹」不需要选中任何东西 —— 空目录里也得能建。
        assert!(remote.iter().any(|e| e.item == MenuItem::NewDir));

        // D5:本地栏一概没有写操作。
        let local = menu_items_for(PanelColumn::Local, None, false);
        assert!(!local.iter().any(|e| e.item == MenuItem::NewDir));
    }

    /// F226:弹框那条路必须**真的删掉**,不能只是没人调。留着的话下一个人
    /// 照着旧代码又接一个入口回去,两套交互再次并存。
    ///
    /// 自证会变红:把 `FileAsk::NewDir` 加回枚举。
    #[test]
    fn the_new_dir_modal_is_gone_not_merely_unreachable() {
        let src = include_str!("files_dialog.rs");
        assert!(
            !src.contains("NewDir {"),
            "files_dialog.rs 里还留着新建文件夹的模态(F226 已改成就地编辑)"
        );
        let panel = include_str!("files_panel.rs");
        assert!(
            !panel.contains("FileAsk::NewDir"),
            "还有人在发 FileAsk::NewDir —— 那个变体已经删了(F226)"
        );
    }
```

> `into_action` 现在是私有的。给它加一个 `#[cfg(test)] pub(crate) fn into_action_for_test(self) -> FileAction { self.into_action() }`，或者直接把测试写在同文件的 `mod tests` 里调 `into_action`（同模块可见私有项）——后者更简单，优先用它，把上面 `into_action_for_test()` 换成 `into_action()`。

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib ui::files_panel 2>&1 | tail -20
```

- [ ] **Step 3：改枚举与菜单**

`crates/mullion-app/src/ui/files_panel.rs`：

1. `FileAsk` 删掉 `NewDir` 变体（连同它的 doc comment）。
2. `MenuItem` 把 `NewFile` 那条旁边加：

```rust
    /// F226:就地新建**文件夹**。同 `NewFile`,不是 `Ask` —— 它不弹框。
    NewDir,
```

3. `menu_items_for` 第一行改成：

```rust
        out.push(on("新建文件夹", MenuItem::NewDir));
        out.push(on("新建文件", MenuItem::NewFile));
```

4. `MenuItem::into_action` 加一条：

```rust
            MenuItem::NewDir => FileAction::BeginNewDir,
```

5. `FileAction` 在 `BeginNewFile` 旁边加两条：

```rust
    /// F226:请求进入就地**新建文件夹**态。同 `BeginNewFile`,纯 UI 状态。
    BeginNewDir,
    /// F226:就地新建文件夹提交。**绝对路径**,在面板里用同一个 `cwd` 拼好 ——
    /// 与 `NewFile` 同一条约定(拼在面板侧,app 侧只管发)。
    NewDir(mullion_ssh::sftp::RemotePath),
```

6. 幽灵行提交那一段（`match new_done` 的 `Some(Some(name))` 分支）改成按 kind 分流：

```rust
        Some(Some(name)) => {
            // **路径在这里拼**,用的是这一帧的 `state.cwd`。
            let path = state.cwd.join(name.as_bytes());
            // F226:发哪一种由**当时那个编辑态的 kind** 决定 —— `newing` 是
            // 这一帧开头 `take` 出来的那份,提交那一刻它还在手上。
            action = Some(match newing.as_ref().map(|n| n.kind) {
                Some(crate::files::state::NewKind::Dir) => FileAction::NewDir(path),
                _ => FileAction::NewFile(path),
            });
        }
```

> `newing` 在 `Some(Some(name))` 这条分支里没被 move（`rename_done` 那边才 `expect` 掉 `renaming`），所以 `as_ref()` 是安全的。若借用检查报错，说明上面某处提前把它 move 了——按编译器提示的实际位置调整，不要猜。

7. 幽灵行渲染处给文件夹画图标：`name_edit_row` 增加一个参数 `icon: Option<crate::ui::file_icon::IconKind>`，函数体里在 `ui.put(...)` **之前**插入：

```rust
    // F226:新建文件夹的幽灵行画一个文件夹图标 —— 输入框里还什么都没有,
    // 图标是用户判断"我在建文件还是建文件夹"的唯一线索。改名行不画
    // (那一行的图标本来就在,由 `row()` 负责),新建文件也不画(维持 F219)。
    if let Some(kind) = icon {
        crate::ui::file_icon::paint(
            ui.painter(),
            icon_rect(row_rect),
            kind,
            theme::c32(crate::ui::file_icon::color_for(kind, true, false, t)),
        );
    }
```

调用点：`rename_row` 里那次传 `None`；幽灵行那次传

```rust
                        (n.kind == crate::files::state::NewKind::Dir)
                            .then_some(crate::ui::file_icon::IconKind::Dir),
```

> `n.kind` 要在 `&mut n.buf` 借用之前先取出来（`let kind = n.kind;`），否则同时可变借用 `n` 会编不过。

- [ ] **Step 4：跑测试确认它绿**

```bash
cargo test -p mullion-app --lib ui::files_panel 2>&1 | tail -5
```

- [ ] **Step 5：删模态**

`crates/mullion-app/src/ui/files_dialog.rs`：删掉 `FilesDialog::NewDir { parent, name }` 变体、它的渲染分支（`modal(ctx, "新建文件夹", ...)` 那一整段）以及只服务于它的辅助代码。**`FileOp::NewDir(RemotePath)` 保留**——就地新建照样发它。

删完按编译错误逐个收口：`app.rs` 里 `FileAsk::NewDir` 的分发分支、`Modal` 完备性表（本仓库的 modal 登记要改**两处**，见 `app.rs::modal_open` 与其穷尽 match 的守护测试）、`files_dialog.rs:742` 与 `1183` 附近的测试。

- [ ] **Step 6：跑绿**

```bash
cargo test -p mullion-app > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
```

- [ ] **Step 7：提交**

```bash
git add -A crates/mullion-app/src
git commit -m "feat(app): 新建文件夹改成列表里就地编辑,删掉模态框 (F226)

菜单项去掉省略号(省略号专指会弹框);幽灵行画文件夹图标;FileAsk::NewDir
与 FilesDialog::NewDir 一并删除,FileOp::NewDir 保留。

守护:ui::files_panel::tests::new_dir_is_an_inline_edit_so_it_carries_no_ellipsis
      ui::files_panel::tests::the_new_dir_modal_is_gone_not_merely_unreachable

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 4.3：app 侧分发 + `Ctrl+Shift+N` + 建完落光标

**Files:**
- Modify: `crates/mullion-app/src/app.rs`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/app.rs` 的 `mod tests`：

```rust
    /// F226:建完之后光标要落到新目录上(同 F219 建文件)。`follow` **必须在
    /// 发请求之前算好** —— 到了完成事件那一刻 `op` 已经被 move 进后台 task,
    /// 拿不回路径了(F219 已经踩过一次,这里是同一条约定的第二个用户)。
    ///
    /// 判据是源码切片:`follow` 那个 match 必须同时覆盖 NewFile 和 NewDir。
    /// 漏掉 NewDir 的表现是"目录建出来了,但列表刷新后光标不知道在哪",
    /// **画面上像是没生效**。
    ///
    /// 自证会变红:把 `FileOp::NewDir(p)` 从那个 match 的臂里去掉。
    #[test]
    fn a_freshly_created_directory_is_revealed_just_like_a_new_file() {
        let src = include_str!("app.rs");
        let anchor = "let follow = match &op {";
        let at = src.find(anchor).expect("找不到 follow 的计算点");
        let seg = &src[at..at + 600];
        assert!(
            seg.contains("FileOp::NewFile(p) | FileOp::NewDir(p)"),
            "新建目录也要 OpFollow::Reveal(F226),否则建完光标不知道落在哪"
        );
    }

    /// F226:Ctrl+Shift+N = 新建文件夹,Ctrl+N = 新建文件。两者都**只在远端栏**
    /// (D5),且必须**各自判 shift** —— 共用一个分支的话,带不带 shift 会落到
    /// 同一个动作上,用户按 Ctrl+Shift+N 建出来的是个文件。
    ///
    /// 自证会变红:把 `mods.shift_key()` 那个三目去掉,恒发 BeginNewFile。
    #[test]
    fn ctrl_shift_n_creates_a_directory_while_ctrl_n_creates_a_file() {
        let src = include_str!("app.rs");
        let anchor = "if s.eq_ignore_ascii_case(\"n\") {";
        let at = src.find(anchor).expect("找不到 Ctrl+N 的分支");
        let seg = &src[at..at + 700];
        assert!(seg.contains("FileAction::BeginNewDir"), "Ctrl+Shift+N 没接上");
        assert!(seg.contains("FileAction::BeginNewFile"), "Ctrl+N 被改坏了");
        assert!(seg.contains("mods.shift_key()"), "两者没按 shift 分流");
    }
```

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib app::tests::a_freshly_created_directory app::tests::ctrl_shift_n 2>&1 | tail -20
```

- [ ] **Step 3：改 `follow` 的计算**（`app.rs:5364` 附近）

```rust
        // F219/F226:建完之后把光标落到新文件/新目录上。**必须在这里算**——
        // 到了完成事件那一刻,`op` 已经被 move 进下面的后台 task 了。
        let follow = match &op {
            FileOp::NewFile(p) | FileOp::NewDir(p) => {
                OpFollow::Reveal(mullion_ssh::sftp::RemotePath::from_bytes(
                    crate::files::reveal::base_name(p.as_bytes(), crate::files::PanelColumn::Remote),
                ))
            }
            _ => OpFollow::None,
        };
```

- [ ] **Step 4：改 Ctrl+N 分支**（`app.rs:5104` 附近）

```rust
                // F219/F226:Ctrl+N 就地新建文件,Ctrl+Shift+N 就地新建文件夹。
                // **只在远端栏**(D5)——焦点在本地栏时静默不动,不是转投远端栏,
                // 理由同 Delete/F2。
                //
                // 用 `eq_ignore_ascii_case`:按住 Shift 时 winit 给的
                // `logical_key` 是大写的 "N",只比小写 "n" 会让 Ctrl+Shift+N
                // 整个落空(静默,像这个键根本没绑)。
                if s.eq_ignore_ascii_case("n") {
                    let column = self
                        .tabs
                        .by_generation(generation)
                        .and_then(|t| t.content.files_panel())
                        .map(|f| f.active_column);
                    if column == Some(crate::ui::files_panel::PanelColumn::Remote) {
                        let action = if mods.shift_key() {
                            FileAction::BeginNewDir
                        } else {
                            FileAction::BeginNewFile
                        };
                        self.dispatch_panel_action(generation, action);
                    }
                    return;
                }
```

- [ ] **Step 5：补齐两处 `FileAction` 的穷尽 match**

- 远端分发（`app.rs:4540` 附近）：`BeginNewDir` 复用 `BeginNewFile` 的形状，调 `files.remote.begin_new_dir()`；`NewDir(path)` 发 `FileOp::NewDir(path.clone())`。

```rust
            // F219/F226:请求进入就地新建态。纯 UI 状态,不发任何请求 ——
            // 与 `Ask` 一样在借出 `files` 之前分流。
            FileAction::BeginNewFile | FileAction::BeginNewDir => {
                let want_dir = matches!(action, FileAction::BeginNewDir);
                if let Some(files) = self
                    .tabs
                    .by_generation_mut(generation)
                    .and_then(|t| t.content.files_panel_mut())
                {
                    if want_dir {
                        files.remote.begin_new_dir();
                    } else {
                        files.remote.begin_new_file();
                    }
                    mark_ui_dirty!(self.ui_dirty);
                    self.request_ui_redraw();
                }
                return;
            }
            // F219/F226:名字定了 —— 路径已经在面板里拼好、也过了两道校验闸,
            // 这里只管发。
            FileAction::NewFile(path) => {
                self.apply_file_op(generation, crate::ui::files_dialog::FileOp::NewFile(path.clone()));
                return;
            }
            FileAction::NewDir(path) => {
                self.apply_file_op(generation, crate::ui::files_dialog::FileOp::NewDir(path.clone()));
                return;
            }
```

> `matches!(action, ...)` 里的绑定名以该函数实际的参数名为准（可能是 `action` 或 `act`）。编译报错时看提示，不要猜。

- 本地栏忽略分支（`app.rs:4403`）：

```rust
            FileAction::BeginNewFile
            | FileAction::NewFile(_)
            | FileAction::BeginNewDir
            | FileAction::NewDir(_) => {
                log::warn!("本地栏收到了新建请求,已忽略(D5)");
                return;
            }
```

- `app.rs:4674` 附近那条「不触发列目录」的穷尽 match 也要补 `| FileAction::BeginNewDir | FileAction::NewDir(_)`。

- [ ] **Step 6：跑绿 + clippy**

```bash
cargo test --workspace > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
```

- [ ] **Step 7：提交，然后做本 Part 全部 6 条 doc comment 里写明的变异验证**

```bash
git add crates/mullion-app/src/app.rs
git commit -m "feat(app): 远端栏 Ctrl+Shift+N 就地新建文件夹,建完落光标 (F226)

守护:app::tests::a_freshly_created_directory_is_revealed_just_like_a_new_file
      app::tests::ctrl_shift_n_creates_a_directory_while_ctrl_n_creates_a_file

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Part 5 — F229：会话克隆

**问题**：想照着一台机器再配一台（同一批代理/自动化/SFTP 偏好，只改 host），只能手工把十几个字段抄一遍。

**为什么不能在 UI 层「填一份草稿 + `editor_id = None` + 保存」**：`EditorBuffer` 里的 `password`/`passphrase`/`proxy_password`/`private_key` **恒为空**——store 不回吐明文（这是 F71/F74 的设计）。保存时靠 `merge_secret(existing, ..)` 把密文重新挂回去，而克隆出来的新 id 的 `existing` 是 `None`，于是**静默产出一份没有凭据的会话**。所以克隆必须在 store 层做，直接深拷贝 `SecretEntry`。

**决定**（已与用户确认）：
- `Vault::clone_session(id, now_rfc3339) -> Result<SessionId, StoreError>`：深拷贝 `SessionRecord` + `SecretEntry`，立刻落盘。
- 命名：`原名 的副本`，撞名再往后 `原名 的副本 2`、`3`……
- 同分组；引用型共享凭据（`Auth::Credential(_)`）**仍然是引用**，不复制凭据实体。
- 菜单位置：右键菜单里「移动到分组」与「删除」之间。**不给快捷键**。
- 克隆完选中新行、打开编辑器、光标落在名称框（复用 `focus_name_request`）。
- 只做**会话**（含 SFTP 页，它就是会话的一个分节）；凭据 / 隧道 / 分组**不做**。

### Task 5.1：`Vault::clone_session`

**Files:**
- Modify: `crates/mullion-store/src/vault.rs`
- Test: 同文件 `mod tests`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-store/src/vault.rs` 的 `mod tests`：

```rust
    /// F229:克隆必须**连密文一起**深拷贝。这是整个特性唯一真正危险的地方 ——
    /// UI 层那条"填草稿再保存"的捷径走不通:`EditorBuffer` 里的密码字段恒为空
    /// (store 不回吐明文),保存时靠 `merge_secret(existing, ..)` 挂回去,而新
    /// id 的 `existing` 是 `None`。结果是一份**没有凭据的会话**,而且完全静默 ——
    /// 用户要等到点「连接」被要密码时才发现。
    ///
    /// 自证会变红:把 `clone_session` 里复制 secret 那一段删掉。
    #[test]
    fn cloning_a_session_deep_copies_its_secret() {
        let dir = tempfile::tempdir().unwrap();
        let mut v = new_vault(&dir);
        let mut draft = sample_draft("prod");
        draft.secret = Some(SecretEntry {
            password: Some("hunter2".into()),
            ..Default::default()
        });
        let src = v.add(draft, "2026-09-08T00:00:00Z");

        let dst = v
            .clone_session(src, "2026-09-08T00:00:01Z")
            .expect("克隆该成功");

        assert_ne!(dst, src, "克隆必须是一条新会话");
        assert_eq!(
            v.secret(dst).and_then(|s| s.password.clone()),
            Some("hunter2".to_owned()),
            "克隆出来的会话没有凭据 —— 用户要到点「连接」时才会发现"
        );
        assert_eq!(
            v.secret(src).and_then(|s| s.password.clone()),
            Some("hunter2".to_owned()),
            "源会话的凭据不该被搬走"
        );
    }

    /// F229:名字要一眼看得出是副本,且**连克隆两次不撞名** —— 列表里两行
    /// 一模一样的名字,用户没有任何办法分辨哪条是哪条。
    ///
    /// 自证会变红:把 `clone_name` 里的去重循环删掉(恒返回「… 的副本」)。
    #[test]
    fn cloning_twice_yields_distinct_names() {
        let dir = tempfile::tempdir().unwrap();
        let mut v = new_vault(&dir);
        let src = v.add(sample_draft("prod"), "2026-09-08T00:00:00Z");

        let a = v.clone_session(src, "2026-09-08T00:00:01Z").unwrap();
        let b = v.clone_session(src, "2026-09-08T00:00:02Z").unwrap();

        let name = |id| {
            v.sessions()
                .iter()
                .find(|r| r.id == id)
                .expect("刚建的会话找得到")
                .identity
                .name
                .clone()
        };
        assert_eq!(name(a), "prod 的副本");
        assert_eq!(name(b), "prod 的副本 2");
    }

    /// F229:克隆保留分组,并且**共享凭据仍然是引用**,不复制凭据实体 ——
    /// 复制一份的话,以后改那条凭据只会改到其中一份,而两条会话在界面上
    /// 长得一模一样(F74 的整个意义就是"一份凭据多会话引用")。
    ///
    /// 自证会变红:把 `clone_session` 里 `rec.identity.group_id` 那份拷贝改成
    /// `None`(第一条红);把 `auth` 改成重建一份 inline(第二条红)。
    #[test]
    fn cloning_keeps_the_group_and_still_references_the_shared_credential() {
        let dir = tempfile::tempdir().unwrap();
        let mut v = new_vault(&dir);
        let g = v.add_group("生产".into());
        let cred = v.add_credential(sample_credential_draft());
        let mut draft = sample_draft("prod");
        draft.identity.group_id = Some(g);
        draft.auth = Auth::Credential(cred);
        let src = v.add(draft, "2026-09-08T00:00:00Z");

        let dst = v.clone_session(src, "2026-09-08T00:00:01Z").unwrap();
        let rec = v.sessions().iter().find(|r| r.id == dst).unwrap();
        assert_eq!(rec.identity.group_id, Some(g));
        assert_eq!(rec.auth, Auth::Credential(cred), "共享凭据必须仍是引用");
        assert_eq!(
            v.credentials().len(),
            1,
            "凭据实体被复制了一份 —— F74 的一份多引用被破坏"
        );
    }

    /// F229:克隆不存在的会话要报 `NotFound`,不能静默造一条空会话出来。
    #[test]
    fn cloning_a_missing_session_is_an_error() {
        let dir = tempfile::tempdir().unwrap();
        let mut v = new_vault(&dir);
        assert!(matches!(
            v.clone_session(SessionId(42), "2026-09-08T00:00:00Z"),
            Err(StoreError::NotFound(SessionId(42)))
        ));
    }
```

> `new_vault(&dir)` / `sample_draft(name)` / `sample_credential_draft()` 这三个辅助函数以该 `mod tests` 里**已有的同类辅助**为准——照抄它现在的名字和签名，不要新造。若没有 `sample_credential_draft`，就用文件里已有的凭据构造方式内联写出来。

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-store 2>&1 | tail -20
```
预期：`no method named 'clone_session'`。

- [ ] **Step 3：实现**

`crates/mullion-store/src/vault.rs`，在 `update` 之后插入：

```rust
    /// F229:克隆一条会话。深拷贝**非敏感字段 + 密文**,落成一条新 id。
    ///
    /// 为什么必须在 store 层做:`EditorBuffer` 里的密码/口令/私钥恒为空
    /// (store 不回吐明文),UI 层"填一份草稿再保存"那条路走到
    /// `merge_secret(existing = None, ..)`,产出的是一份**没有凭据的会话**,
    /// 而且完全静默 —— 用户要等到点「连接」被要密码时才发现。
    ///
    /// `Auth::Credential(_)` 原样带过去:那是**引用**,复制凭据实体会让
    /// 「一份凭据多会话引用」(F74)当场失效。
    pub fn clone_session(
        &mut self,
        id: SessionId,
        now_rfc3339: &str,
    ) -> Result<SessionId, StoreError> {
        self.sync_from_disk_if_untouched();
        let src = self
            .sessions
            .iter()
            .find(|s| s.id == id)
            .ok_or(StoreError::NotFound(id))?;
        let mut rec = src.clone();
        let new_id = SessionId(self.sessions.iter().map(|s| s.id.0).max().unwrap_or(0) + 1);
        let taken: Vec<&str> = self.sessions.iter().map(|s| s.identity.name.as_str()).collect();
        rec.identity.name = clone_name(&src.identity.name, &taken);
        rec.id = new_id;
        rec.modified_at = now_rfc3339.to_string();
        // 密文与非敏感部分**同进同出**。`SecretEntry: Clone`,这里不 `{:?}`
        // 打印它的任何一部分(它连 Debug 都没 derive,正是为了防这个)。
        if let Some(sec) = self.secrets.get(&id.0.to_string()).cloned() {
            self.secrets.insert(new_id.0.to_string(), sec);
        }
        self.sessions.push(rec);
        Ok(new_id)
    }
```

在同文件的自由函数区（`impl Vault` 之外）加：

```rust
/// F229:给克隆出来的会话起名。撞名就往后编号 ——「prod 的副本」「prod 的副本 2」…
///
/// 纯函数:去重是这里唯一容易写错的地方,而它跟磁盘、时钟、加密都无关。
/// 连克隆两次撞出两行同名会话的话,用户在列表里没有任何办法分辨。
fn clone_name(base: &str, taken: &[&str]) -> String {
    let first = format!("{base} 的副本");
    if !taken.contains(&first.as_str()) {
        return first;
    }
    // 从 2 起编号。上限只是防呆:名字撞到 u32 上限说明调用方在死循环。
    for n in 2..u32::MAX {
        let candidate = format!("{base} 的副本 {n}");
        if !taken.contains(&candidate.as_str()) {
            return candidate;
        }
    }
    first
}
```

- [ ] **Step 4：把闸门守护的下界抬一格**

`crates/mullion-store/src/vault.rs` 的 `every_mutating_entry_point_reloads_before_it_writes`，把 `checked >= 24` 改成 `checked >= 25`，并在断言消息不变的前提下确认 `clone_session` 的第一句就是 `self.sync_from_disk_if_untouched();`（上面的实现已经是了）。

- [ ] **Step 5：跑绿**

```bash
cargo test -p mullion-store 2>&1 | tail -8
```

- [ ] **Step 6：提交**

```bash
git add crates/mullion-store/src/vault.rs
git commit -m "feat(store): Vault::clone_session 深拷贝会话与密文 (F229)

必须在 store 层做:EditorBuffer 里的凭据字段恒为空(不回吐明文),UI 层
「填草稿再保存」会走到 merge_secret(existing = None),静默产出一份没有
凭据的克隆。共享凭据保持引用,不复制凭据实体(F74)。

守护:vault::tests::cloning_a_session_deep_copies_its_secret
      vault::tests::cloning_twice_yields_distinct_names
      vault::tests::cloning_keeps_the_group_and_still_references_the_shared_credential

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 5.2：右键菜单「克隆」 + app 侧接线

**Files:**
- Modify: `crates/mullion-app/src/ui/mod.rs`（新增 `clone_request` 字段）
- Modify: `crates/mullion-app/src/ui/session_manager/list.rs`（菜单项）
- Modify: `crates/mullion-app/src/app.rs`（施加点）

- [ ] **Step 1：加意图字段**

`crates/mullion-app/src/ui/mod.rs`，在 `move_to_group` 旁边：

```rust
    /// F229:右键「克隆」的意图。同 `move_to_group` —— UI 只写意图,真正动
    /// store 在 `app.rs` 的统一施加点(egui 闭包里借不到 `&mut self.store`)。
    pub clone_request: Option<SessionId>,
```

- [ ] **Step 2：写失败的测试**

加到 `crates/mullion-app/src/ui/session_manager/list.rs` 的 `mod tests`（照着已有的 `right_click_offers_connect_and_move_to_group` 那条的驱动方式写，**不要**另造一套 harness）：

```rust
    /// F229:右键菜单里要有「克隆」,位置在「移动到分组」与「删除」之间 ——
    /// 紧挨着删除是有意的:两者都是"对这一行整体"的操作;排在连接类之后
    /// 是因为它不是日常动作。
    ///
    /// 自证会变红:把 `ui_state.clone_request = Some(rec.id);` 那一行删掉,
    /// 末尾的 assert_eq 报 `None`。
    #[test]
    fn right_click_offers_clone_between_move_to_group_and_delete() {
        // …照抄 `right_click_offers_connect_and_move_to_group` 的构造与驱动，
        // 把它点的按钮换成「克隆」…
        assert_eq!(ui_state.clone_request, Some(SessionId(1)));
    }
```

> 这条测试的**具体驱动代码必须照抄同文件里 `right_click_offers_connect_and_move_to_group` 的写法**（它已经解决了「egui 的 context_menu 要一次右键 + 一帧才展开」这个难点）。照抄之后只改两处：点的文字换成 `"克隆"`，末尾断言换成上面这句。不要自己发明驱动方式——本仓库为 egui 预热帧数不足吃过两次静默假绿。

- [ ] **Step 3：加菜单项**

`crates/mullion-app/src/ui/session_manager/list.rs`，在 `ui.menu_button("移动到分组", ...)` 那一段**之后**、`if ui.button("删除")` 之前插入：

```rust
        // F229:克隆。整份配置(含密文)复制成一条新会话 —— 手工抄十几个字段
        // 又慢又容易漏,而漏掉的多半是代理/自动化这种"不连上根本发现不了"的。
        if ui.button("克隆").clicked() {
            ui_state.clone_request = Some(rec.id);
            ui.close_menu();
        }
```

- [ ] **Step 4：app 侧施加**

`crates/mullion-app/src/app.rs`：

1. `touched_store` 那个表达式加一项（**外观缓存必须重算**：克隆出来的新会话在列表里要有图标/颜色）：

```rust
                    // F229:克隆会新增一行会话,外观缓存必须跟着重算 ——
                    // 漏掉的话新行画的是默认色/默认图标。
                    || self.ui.clone_request.is_some()
```

2. 打 `StoreIo` 标记的那个条件也加 `|| self.ui.clone_request.is_some()`。

3. 在 `move_to_group` 的施加块**之后**插入：

```rust
                // F229:右键「克隆」。深拷贝在 store 层(见 `Vault::clone_session`
                // 的文档:UI 层那条捷径会静默丢掉凭据),这里只负责落盘、报结果、
                // 把新行选中并把光标送进名称框 —— 克隆出来第一件事必然是改名。
                if let Some(id) = self.ui.clone_request.take() {
                    if let Some(store) = self.store.as_mut() {
                        let now = time::OffsetDateTime::now_utc()
                            .format(&time::format_description::well_known::Rfc3339)
                            .unwrap_or_default();
                        match store
                            .clone_session(id, &now)
                            .and_then(|new_id| store.save().map(|()| new_id))
                        {
                            Ok(new_id) => {
                                self.ui.pending_switch = Some(
                                    crate::ui::session_manager::SwitchTarget::Session(new_id),
                                );
                                self.ui.focus_name_request = true;
                                self.ui
                                    .set_toast(crate::ui::toast::Kind::Ok, "已克隆会话");
                            }
                            Err(e) => self.ui.set_error(format!("克隆失败:{e}")),
                        }
                    }
                }
```

> `SwitchTarget` 的实际导出路径以 `session_manager/mod.rs` 为准。`pending_switch` 会在会话管理器渲染时被 `switch_to` 消费（`session_manager/mod.rs:900` 附近），它顺带重置 `editor_baseline`/`editor_tab`——所以这里**不要**自己去写 `editor_id`，否则基线不同步，刚克隆出来的表单会立刻被判成脏。

- [ ] **Step 5：跑绿 + clippy**

```bash
cargo test --workspace > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
```

- [ ] **Step 6：提交，然后做本 Part 5 条 doc comment 里写明的变异验证**

```bash
git add crates/mullion-app/src crates/mullion-store/src
git commit -m "feat(app): 会话列表右键「克隆」 (F229)

守护:ui::session_manager::list::tests::right_click_offers_clone_between_move_to_group_and_delete

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Part 6 — F227：远端栏内拖拽到目录（移动 / Ctrl 复制）

**问题**：远端栏内把文件拖到一个目录上什么也不发生。`drop_target` 里那道 `if from == onto { return None; }` 明确挡着，注释也写清了为什么必须挡——放过去会走成「把远端文件上传到它自己」，`open_write` 截断在读之前，**文件直接清零**。

**决定**（已与用户确认）：
- 那道闸只对**本地栏**保留（`from == onto && onto == Local` → 拒绝；本地文件管理外包给资源管理器，D5）。
- 远端栏内的拖放**不走传输通路**，改成路由进 F220 的粘贴流水线（`RemoteClip` → `start_paste_of`）。这样 `is_within` 自嵌套闸、冲突弹框、`paste_seq` 过期闸**全部白拿**，且完全绕开了「上传给自己」那条危险路径。
- **默认移动，按住 Ctrl 是复制**。
- 只有落在**目录行**上才成立；落在空白 / 路径条 / 文件行上一律**无操作**（那等于「原地移动」）。
- 不弹二次确认框，用 toast 报结果。
- 拖拽预览胶囊写「移动 3 项到 logs」/「复制 3 项到 logs」。

> **不复用 `files.clip`**：拖出来的这一批是**临时**的剪贴板，用户手上真正的 Ctrl+C 内容不许被它顶掉。合成一个一次性的 `RemoteClip` 传下去即可；完成时 `OpFollow::ClearClip(pasted)` 是**按值**跟当时的 `files.clip` 比对的（`follow_for_clip_mode` 的既有语义），两者不相等，所以不会误清用户的剪贴板。

### Task 6.1：拖拽判据改成 `drop_intent`

**Files:**
- Modify: `crates/mullion-app/src/files/drag.rs`

- [ ] **Step 1：写失败的测试**

替换 `crates/mullion-app/src/files/drag.rs` 里 `dragging_within_the_same_column_is_not_a_transfer` 那条，并加新的：

```rust
    /// F227:本地栏内拖仍然不成立 —— 本机文件管理外包给资源管理器(D5)。
    #[test]
    fn dragging_within_the_local_column_is_still_refused() {
        assert_eq!(
            drop_intent(
                PanelColumn::Local,
                PanelColumn::Local,
                Some(b"sub".to_vec()),
                false
            ),
            None
        );
    }

    /// F227:远端栏内拖到目录行 = **移动**;按住 Ctrl = 复制。
    ///
    /// 这两条必须分开断言:共用一条的话,把 `ctrl` 那个三目写反了(移动/复制
    /// 对调)照样只有一条断言在管,而**移动是破坏性的** —— 用户以为复制了一份,
    /// 源目录里那份其实已经没了。
    ///
    /// 自证会变红:把 `ctrl` 的两个分支对调,两条各自红一半。
    #[test]
    fn dragging_within_the_remote_column_moves_and_ctrl_copies() {
        use crate::files::clip::ClipMode;
        assert_eq!(
            drop_intent(
                PanelColumn::Remote,
                PanelColumn::Remote,
                Some(b"logs".to_vec()),
                false
            ),
            Some((DropKind::Within(ClipMode::Cut), Landing::Sub(b"logs".to_vec())))
        );
        assert_eq!(
            drop_intent(
                PanelColumn::Remote,
                PanelColumn::Remote,
                Some(b"logs".to_vec()),
                true
            ),
            Some((DropKind::Within(ClipMode::Copy), Landing::Sub(b"logs".to_vec())))
        );
    }

    /// F227:远端栏内落在空白 / 路径条 / 文件行上一律不成立 —— 目标会是
    /// 「当前目录」,那等于原地移动,一个无操作。当成一次真的粘贴发出去,
    /// 用户会以为自己干了点什么(而且会真的收到一句"源和目标是同一个目录")。
    ///
    /// 自证会变红:把 `let name = over_dir?;` 改成 `unwrap_or_default()`。
    #[test]
    fn dropping_inside_the_remote_column_on_anything_but_a_directory_is_a_noop() {
        assert_eq!(
            drop_intent(PanelColumn::Remote, PanelColumn::Remote, None, false),
            None
        );
    }

    /// F227:跨栏的语义一个字没变(F58) —— 目录行进子目录,空白进当前目录。
    #[test]
    fn cross_column_drops_keep_their_transfer_semantics() {
        assert_eq!(
            drop_intent(
                PanelColumn::Local,
                PanelColumn::Remote,
                Some(b"logs".to_vec()),
                false
            ),
            Some((DropKind::Transfer, Landing::Sub(b"logs".to_vec())))
        );
        assert_eq!(
            drop_intent(PanelColumn::Remote, PanelColumn::Local, None, true),
            Some((DropKind::Transfer, Landing::Cwd)),
            "跨栏时 Ctrl 不改变语义 —— 上传/下载本来就是复制"
        );
    }

    /// F227:胶囊在栏内拖时要说清**是移动还是复制、落到哪** —— 这两件事都是
    /// 松手之前唯一的提示(不弹二次确认框)。
    #[test]
    fn the_drag_capsule_spells_out_move_versus_copy_and_the_target() {
        use crate::files::clip::ClipMode;
        assert_eq!(
            preview_label(3, "a.txt", Some(("logs", ClipMode::Cut))),
            "移动 3 项到 logs"
        );
        assert_eq!(
            preview_label(3, "a.txt", Some(("logs", ClipMode::Copy))),
            "复制 3 项到 logs"
        );
        // 跨栏(没悬在目录上)维持 F151 原样。
        assert_eq!(preview_label(1, "a.txt", None), "a.txt");
        assert_eq!(preview_label(3, "a.txt", None), "拖动 3 项");
    }
```

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib files::drag 2>&1 | tail -20
```

- [ ] **Step 3：实现**

`crates/mullion-app/src/files/drag.rs`，把 `drop_target` 整个替换成：

```rust
/// F227:一次拖拽落地之后到底要做什么。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum DropKind {
    /// 跨栏 —— 上传 / 下载(F58 的既有语义)。
    Transfer,
    /// 远端栏内 —— 移动(默认)或复制(按住 Ctrl)。走 F220 的粘贴流水线,
    /// **不走传输通路**。
    Within(crate::files::clip::ClipMode),
}

/// 从 `from` 栏拖到 `onto` 栏、松手时指针底下是 `over_dir`、当时按着 Ctrl 没有,
/// 该做什么、落到哪儿。`None` = 这一拖不成立。
///
/// `over_dir`:松手那一刻指针底下那一行的名字,**且那一行是目录**。文件行与
/// 空白都给 `None` —— 落在文件上不解释成「覆盖那个文件」:拖拽没有二次确认,
/// 一个手抖就能把远端文件盖掉,而「覆盖某个具体文件」这个需求本来就罕见。
///
/// # 为什么栏内拖不能走传输通路
///
/// 远端栏内把 a 拖到 b,如果解释成「把 a 上传到远端 b」,源和目标是同一条
/// SFTP 连接上的同一棵树 —— `open_write` 截断在前、读在后,**文件直接清零**。
/// 所以栏内拖走的是完全不同的一条路:合成一份 `RemoteClip` 交给 F220 的
/// 粘贴流水线(`is_within` 自嵌套闸、冲突弹框、序号过期闸全在那条路上)。
///
/// 本地栏内拖仍然一律拒绝:本机文件管理外包给资源管理器(设计 D5)。
pub fn drop_intent(
    from: PanelColumn,
    onto: PanelColumn,
    over_dir: Option<Vec<u8>>,
    ctrl: bool,
) -> Option<(DropKind, Landing)> {
    use crate::files::clip::ClipMode;
    if from == onto {
        if onto == PanelColumn::Local {
            return None;
        }
        // 只有落在**目录行**上才成立。落在空白/路径条/文件行上,目标就是
        // 当前目录 —— 那等于「原地移动」,一个无操作;当成一次真的粘贴发
        // 出去,用户会以为自己干了点什么。
        let name = over_dir?;
        let mode = if ctrl { ClipMode::Copy } else { ClipMode::Cut };
        return Some((DropKind::Within(mode), Landing::Sub(name)));
    }
    // 跨栏:Ctrl 不改变语义 —— 上传/下载本来就是复制。
    Some((
        DropKind::Transfer,
        match over_dir {
            Some(name) => Landing::Sub(name),
            None => Landing::Cwd,
        },
    ))
}
```

`preview_label` 改成：

```rust
/// F151/F227:拖拽途中跟着指针走的那个小胶囊上写什么。
///
/// `into`:栏内拖时指针正压着的目标目录 + 这一拖是移动还是复制。有它就必须
/// 写出来 —— 栏内拖**不弹二次确认框**,松手前这句话是用户唯一的提示,而
/// 「移动」是破坏性的。跨栏拖给 `None`,维持 F151 原样。
///
/// 单项显名字(用户要确认拖的是哪一个),多项显条数(名字列表在指针边上
/// 铺不下,而且这时用户关心的是「有没有把该选的都带上」)。
pub fn preview_label(
    n: usize,
    first: &str,
    into: Option<(&str, crate::files::clip::ClipMode)>,
) -> String {
    use crate::files::clip::ClipMode;
    match into {
        Some((dir, ClipMode::Cut)) => format!("移动 {n} 项到 {dir}"),
        Some((dir, ClipMode::Copy)) => format!("复制 {n} 项到 {dir}"),
        None if n <= 1 => first.to_owned(),
        None => format!("拖动 {n} 项"),
    }
}
```

同文件里 `preview_label` 的既有两条测试（`drag.rs:225/233`）补上第三个实参 `None`。

- [ ] **Step 4：跑测试确认它绿**

```bash
cargo test -p mullion-app --lib files::drag 2>&1 | tail -5
```

- [ ] **Step 5：提交**

```bash
git add crates/mullion-app/src/files/drag.rs
git commit -m "feat(app): 拖拽判据改 drop_intent,放行远端栏内拖(移动/Ctrl 复制) (F227)

守护:files::drag::tests::dragging_within_the_remote_column_moves_and_ctrl_copies
      files::drag::tests::dropping_inside_the_remote_column_on_anything_but_a_directory_is_a_noop

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 6.2：面板接线（落点 + 胶囊）

**Files:**
- Modify: `crates/mullion-app/src/ui/files_panel.rs`

- [ ] **Step 1：加动作变体**

`FileAction` 里，`Drop(Landing)` 旁边加：

```rust
    /// F227:远端栏**栏内**拖放。`into` 是当前目录下那个目标子目录的名字,
    /// `mode` 是移动还是复制。**不是 `Drop`** —— 它走的是 F220 的粘贴
    /// 流水线,不是传输通路(理由见 `drag::drop_intent` 的文档)。
    DropWithin {
        into: Vec<u8>,
        mode: crate::files::clip::ClipMode,
    },
```

- [ ] **Step 2：改行落点**（`files_panel.rs:1073` 附近）

```rust
                if let Some(from) = resp.dnd_release_payload::<crate::files::drag::DragFrom>() {
                    let over = (e.kind == EntryKind::Dir && e.name.is_operable())
                        .then(|| e.name.as_bytes().to_vec());
                    // Ctrl 只在**松手那一刻**读一次 —— 拖动途中按下又松开
                    // 不该改变已经决定的语义。
                    let ctrl = ui.input(|i| i.modifiers.ctrl);
                    intent = crate::files::drag::drop_intent(from.0, column, over, ctrl);
                }
```

同时把这一段之上、行循环里用来给胶囊报目标的悬停记录也记下来（**在 `dnd_release_payload` 之前**，因为它会把载荷取走）：

```rust
                // F227:胶囊要报「落到哪」,得知道指针此刻压着哪个目录行。
                // 只在**栏内拖**时有意义(跨栏时源栏这边的指针根本不在本栏)。
                if outgoing && resp.hovered() && e.kind == EntryKind::Dir && e.name.is_operable() {
                    hover_dir = Some(e.name.display().to_string());
                }
```

在行循环之前声明：

```rust
    let mut intent: Option<(crate::files::drag::DropKind, crate::files::drag::Landing)> = None;
    let mut hover_dir: Option<String> = None;
```

（原来的 `let mut landing: Option<...> = None;` 删掉。）

- [ ] **Step 3：改空白落点**（`files_panel.rs:1181` 附近）

```rust
    if intent.is_none() {
        if let Some(from) = bg.dnd_release_payload::<crate::files::drag::DragFrom>() {
            let ctrl = ui.input(|i| i.modifiers.ctrl);
            intent = crate::files::drag::drop_intent(from.0, column, None, ctrl);
        }
    }
    match intent {
        Some((crate::files::drag::DropKind::Transfer, l)) => action = Some(FileAction::Drop(l)),
        // 栏内拖只可能落在目录行上(`drop_intent` 已经挡掉了其余情况),
        // `Landing::Cwd` 走到这里说明那道闸被改坏了 —— 不静默吞。
        Some((crate::files::drag::DropKind::Within(mode), l)) => {
            if let Some(into) = l.sub() {
                action = Some(FileAction::DropWithin { into, mode });
            } else {
                log::warn!("栏内拖放落到了当前目录,已忽略(F227:只接目录行)");
            }
        }
        None => {}
    }
```

- [ ] **Step 4：胶囊挪到行循环之后并带上目标**

把 `files_panel.rs:640-664` 那整段 `if outgoing { ... }` **原封不动搬到行循环（`ScrollArea` 那一整块）之后**，并把 `label` 那一行改成：

```rust
            // F227:栏内拖时把「移动/复制 + 目标目录」写在胶囊上 —— 栏内拖
            // 不弹二次确认框,这句话是松手之前唯一的提示。
            let ctrl = ui.input(|i| i.modifiers.ctrl);
            let mode = if ctrl {
                crate::files::clip::ClipMode::Copy
            } else {
                crate::files::clip::ClipMode::Cut
            };
            let into = hover_dir.as_deref().map(|d| (d, mode));
            let label = crate::files::drag::preview_label(state.selected.len(), &first, into);
```

> `outgoing` 的定义（`payload().is_some_and(|f| f.0 == column)`）保持不动。搬位置是必须的：`hover_dir` 只有跑完行循环才有值，在原位置读到的永远是 `None`（**静默**——胶囊照旧显示「拖动 3 项」，看不出是 bug 还是「我没悬在目录上」）。

- [ ] **Step 5：跑绿**

```bash
cargo test -p mullion-app > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
```

- [ ] **Step 6：提交**

```bash
git add crates/mullion-app/src/ui/files_panel.rs
git commit -m "feat(app): 远端栏内拖到目录行发 DropWithin,胶囊写明移动/复制与目标 (F227)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 6.3：app 侧路由进粘贴流水线

**Files:**
- Modify: `crates/mullion-app/src/app.rs`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/app.rs` 的 `mod tests`：

```rust
    /// F227:栏内拖放**必须**走 F220 的粘贴流水线,不能自己再写一条。走那条路
    /// 白拿三道已经验过的闸:`is_within`(把目录拖进自己的子目录会边列边写,
    /// 递归到写满磁盘)、冲突弹框、`paste_seq` 过期闸。自己写一条的话这三道
    /// 要重新实现一遍,而**头一道漏了会写满远端磁盘**。
    ///
    /// 判据是调用形态:`DropWithin` 的处理必须落到 `start_paste_of`。
    ///
    /// 自证会变红:把那个分支改成直接调 `start_transfer_into`。
    #[test]
    fn a_within_column_drop_goes_through_the_paste_pipeline() {
        let src = include_str!("app.rs");
        let anchor = "FileAction::DropWithin { into, mode }";
        let at = src.find(anchor).expect("找不到 DropWithin 的处理");
        let seg = &src[at..at + 900];
        assert!(
            seg.contains("start_paste_of("),
            "栏内拖放没走粘贴流水线 —— is_within 自嵌套闸会一并丢掉(F227)"
        );
        assert!(
            !seg.contains("start_transfer_into("),
            "栏内拖放走了传输通路 —— 那是「把远端文件上传给它自己」,文件会被清零"
        );
    }

    /// F227:合成的那份剪贴板**不许**写进 `files.clip` —— 用户手上真正的
    /// Ctrl+C 内容会被一次拖拽悄悄顶掉,而他下一次 Ctrl+V 粘出来的是拖过的
    /// 那批文件。
    ///
    /// 自证会变红:在 `DropWithin` 分支里加一句 `files.clip = Some(clip.clone());`。
    #[test]
    fn a_drag_does_not_clobber_the_users_clipboard() {
        let src = include_str!("app.rs");
        let anchor = "FileAction::DropWithin { into, mode }";
        let at = src.find(anchor).expect("找不到 DropWithin 的处理");
        let seg = &src[at..at + 900];
        assert!(
            !seg.contains("clip = Some("),
            "拖拽合成的临时剪贴板被写进了面板 —— 用户的 Ctrl+C 内容会被顶掉"
        );
    }
```

- [ ] **Step 2：把 `start_paste` 拆成两层**

`crates/mullion-app/src/app.rs`：

```rust
    /// F220:Ctrl+V —— 读面板此刻的剪贴板与当前目录,交给 `start_paste_of`。
    ///
    /// 冻结在**这里**发生(见 `start_paste_of` 的文档):用户按下 Ctrl+V 那一刻,
    /// 这次操作往哪儿粘、粘什么就已经定了。
    fn start_paste(&mut self, generation: u64) {
        let Some(files) = self
            .tabs
            .by_generation(generation)
            .and_then(|t| t.content.files_panel())
        else {
            return;
        };
        let Some(clip) = files.clip.clone() else {
            self.ui.set_toast(
                crate::ui::toast::Kind::Warn,
                "剪贴板是空的 —— 先在远端栏复制或剪切",
            );
            return;
        };
        let dst = files.remote.cwd.clone();
        self.start_paste_of(generation, clip, dst);
    }

    /// F220/F227:把**已经冻结好的** `clip` 粘到**已经冻结好的** `dst`。
    ///
    /// 两个来源:Ctrl+V(`start_paste`,取面板的剪贴板与当前目录)和栏内拖放
    /// (F227,合成一份一次性的 `RemoteClip`、目标是指针底下那个目录)。
    /// 共用这一层是有意的 —— 下面三道闸(自嵌套、同目录剪切空操作、序号过期)
    /// 各写一遍的话,拖放那条路上没有任何东西会让用户发现两边算得不一样,
    /// 而**头一道漏了会把远端磁盘写满**。
    ///
    /// 复核 C1/I2:`dst`/`clip` 原样随事件带走 —— `accept_paste_check` 不再去
    /// 读 `files.remote.cwd`/`files.clip`。之后用户在界面上怎么走,都不该改变
    /// 一次已经发出去的操作的语义。
    fn start_paste_of(
        &mut self,
        generation: u64,
        clip: crate::files::clip::RemoteClip,
        dst: mullion_ssh::sftp::RemotePath,
    ) {
        // …这里原样保留 `start_paste` 原有的函数体,从「**闸门排在任何请求
        // 之前**」那段注释开始,直到结尾的 `set_toast(Busy, "正在检查目标目录…")`。
        // 需要 `tab`/`files` 的两处(`begin_paste`、`sftp_client`)重新取一次:
        //
        //     let Some(tab) = self.tabs.by_generation_mut(generation) else { return; };
        //     let Some(files) = tab.content.files_panel_mut() else { return; };
        //
        // 取的位置就在 `let seq = files.remote.begin_paste();` 之前。
    }
```

- [ ] **Step 3：接 `DropWithin`**

在远端栏的 `FileAction` 分发里（`FileAction::Drop(_)` 附近），加：

```rust
            // F227:远端栏内拖到目录行。合成一份**一次性**的剪贴板交给粘贴
            // 流水线 —— 不写回 `files.clip`,用户手上真正的 Ctrl+C 内容不许
            // 被一次拖拽顶掉。
            FileAction::DropWithin { into, mode } => {
                let (clip, dst) = {
                    let Some(files) = self
                        .tabs
                        .by_generation(generation)
                        .and_then(|t| t.content.files_panel())
                    else {
                        return;
                    };
                    let items: Vec<_> = files
                        .remote
                        .selected_entries()
                        .map(|e| (files.remote.cwd.join(e.name.as_bytes()), e.kind == EntryKind::Dir))
                        .collect();
                    if items.is_empty() {
                        return;
                    }
                    (
                        crate::files::clip::RemoteClip { mode: *mode, items },
                        files.remote.cwd.join(into),
                    )
                };
                self.start_paste_of(generation, clip, dst);
                return;
            }
```

> `selected_entries()` 的实际名字以 `PaneState` 现有的 API 为准（F220 的 `ClipCopy` 分支就是这么取选中集 + 「是不是目录」的——**照抄它那一段**，不要另写一遍；两边算得不一样时，拖放这条路上没人会发现）。`into` 与 `mode` 的 `*`/`clone()` 按分发函数里 `action` 是值还是引用调整。

- [ ] **Step 4：补穷尽 match**

`app.rs` 里所有列了 `FileAction::Drop(_)` 的穷尽 match（本地栏忽略分支、「不触发列目录」那条）都要补上 `| FileAction::DropWithin { .. }`。本地栏那条要单独说明：

```rust
            // F227:本地栏内拖不成立(`drop_intent` 挡在前面,D5)。走到这儿
            // 说明那道闸被改坏了 —— 不静默吞。
            FileAction::DropWithin { .. } => {
                log::warn!("本地栏收到了栏内拖放,已忽略(D5)");
                return;
            }
```

- [ ] **Step 5：跑绿 + clippy**

```bash
cargo test --workspace > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
```

- [ ] **Step 6：提交，然后做本 Part 全部 7 条 doc comment 里写明的变异验证**

```bash
git add crates/mullion-app/src/app.rs
git commit -m "feat(app): 远端栏内拖放路由进 F220 粘贴流水线 (F227)

不走传输通路 —— 那是「把远端文件上传给它自己」,open_write 截断在读之前,
文件会被清零。走粘贴那条路白拿 is_within 自嵌套闸、冲突弹框、序号过期闸。
合成的剪贴板是一次性的,不写回 files.clip。

守护:app::tests::a_within_column_drop_goes_through_the_paste_pipeline
      app::tests::a_drag_does_not_clobber_the_users_clipboard

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Part 7 — F230：SFTP 文件在两个 Mullion 之间复制粘贴

**问题**：F220 的剪贴板是**进程内**的一个 `Option<RemoteClip>`。开两个 Mullion（两台机器各一个窗口）时，A 里 Ctrl+C、B 里 Ctrl+V 什么也不会发生。

**决定**（已与用户确认）：

1. **系统剪贴板是唯一真值**。进程内那份 `files.clip` 降级为「画淡剪切源」的显示用途，粘贴一律读系统剪贴板——否则会出现「我刚复制了新东西，粘出来的却是旧的」。
2. `Ctrl+C` / `Ctrl+X` 往系统剪贴板写**两种格式**：
   - **Mullion 私有格式**（`RegisterClipboardFormatW("MullionRemoteFiles")`）：带**远端主机密钥指纹** + 远端绝对路径 + copy/cut。
   - **虚拟文件格式**（`FileGroupDescriptorW` + `FileContents`，沿用 F59 的实现）：让资源管理器/别的程序也能粘。
3. `Ctrl+V` 分三路：
   - 系统剪贴板里有 Mullion 私有格式，且**指纹与本标签当前连接一致** → **快路径**：直接在远端 `copy_tree`/`rename`，零传输。
   - 有私有格式但指纹不同 → **慢路径**：先下载到临时目录再上传。
   - 只有 `CF_HDROP`（用户在资源管理器里 Ctrl+C 的本地文件）→ **上传**，复用 F52 的 `group_by_parent` + `plan_transfer`。
4. **剪切在慢路径上降级为复制**：源保留，用 toast 说明理由。跨机器的「移动」要在源端删，而删除不可逆、且源端那条连接可能已经断了。对外一律声明 `DROPEFFECT_COPY`。
5. **支持目录**。快路径天然支持（`copy_tree`）；虚拟文件那条路在 `GetData` 回调里**递归展开**——那时用户已经按下 Ctrl+V，等几秒是可接受的，而起拖那一刻卡几十秒不可接受（这正是 F59 当初跳过目录的理由，粘贴场景下不成立）。`descriptor.rs` 因此要能写**相对路径**（`sub\dir\file.txt`）。
6. **接受「Mullion 一关，剪贴板就失效」**——不做 `OleFlushClipboard`（渲染整棵目录树到剪贴板可能是几百 MB）。

> **平台边界**：整个系统剪贴板部分 `#[cfg(windows)]`。非 Windows 上退回 F220 的进程内行为（`files.clip`），编得过即可。
>
> **你验证不了的部分**（写进 PR 的人工验收清单，不要说「已完成」）：OLE 剪贴板在真实资源管理器 / 别的 Mullion 实例之间的行为、延迟渲染回调的时序、目录递归展开在大树上的表现。

### Task 7.1：私有剪贴板格式的纯编解码

**Files:**
- Create: `crates/mullion-app/src/clipboard_remote.rs`
- Modify: `crates/mullion-app/src/lib.rs`（加 `pub mod clipboard_remote;`）

- [ ] **Step 1：写失败的测试**

新建 `crates/mullion-app/src/clipboard_remote.rs`，先只写测试模块（实现留空，让它编译失败）：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::files::clip::{ClipMode, RemoteClip};
    use mullion_ssh::sftp::RemotePath;

    fn clip(mode: ClipMode) -> RemoteClip {
        RemoteClip {
            mode,
            items: vec![
                (RemotePath::from_bytes(b"/srv/app/a.txt".to_vec()), false),
                (RemotePath::from_bytes(b"/srv/app/logs".to_vec()), true),
            ],
        }
    }

    /// F230:编码 → 解码必须原样回来,**包括路径的原始字节**。远端文件名不保证
    /// 是 UTF-8(这条在本项目里是硬事实,`RemotePath` 整个类型就是为它存在的),
    /// 走一趟 `String` 会把非 UTF-8 的名字换成替换字符 —— 粘贴时打不中那个文件,
    /// 报一句 `NoSuchFile`,而用户在列表里明明看得见它。
    ///
    /// 自证会变红:把 `encode` 里写路径那段换成 `p.display().as_bytes()`。
    #[test]
    fn a_payload_round_trips_including_non_utf8_names() {
        let mut c = clip(ClipMode::Cut);
        c.items
            .push((RemotePath::from_bytes(vec![b'/', 0xFF, 0xFE]), false));
        let bytes = encode("SHA256:abc", &c);
        let got = decode(&bytes).expect("刚编出来的必须解得开");
        assert_eq!(got.fingerprint, "SHA256:abc");
        assert_eq!(got.clip, c);
    }

    /// F230:别人的剪贴板内容(或者格式换代之后的旧数据)必须**解不开**,
    /// 而不是解出半份垃圾。解出半份的后果是拿一批乱码路径去远端发请求。
    #[test]
    fn foreign_or_truncated_payloads_are_rejected() {
        assert!(decode(b"").is_none());
        assert!(decode(b"not a mullion payload").is_none());
        let good = encode("SHA256:abc", &clip(ClipMode::Copy));
        assert!(decode(&good[..good.len() - 1]).is_none(), "截断的载荷不许解开");
    }

    /// F230 的核心判据:指纹相同才走快路径(远端直接 copy/rename,零传输)。
    ///
    /// 指纹取自 `known_hosts` 里这台机器那条记录 —— `check_server_key` 过了
    /// 就意味着「实测指纹 == 记录指纹」,两者等价。指纹对不上 = 不是同一台
    /// 机器,远端根本看不到那些路径,直接 copy 会打到**本机上同名的另一个
    /// 文件**(路径是绝对的,而 `/srv/app` 这种路径在两台机器上都存在) ——
    /// 静默复制错文件,这是这一片最危险的失误。
    ///
    /// 自证会变红:把 `route_for` 的条件反过来。
    #[test]
    fn only_a_matching_fingerprint_takes_the_fast_path() {
        assert_eq!(route_for("SHA256:abc", Some("SHA256:abc")), Route::Fast);
        assert_eq!(route_for("SHA256:abc", Some("SHA256:zzz")), Route::Slow);
        // 本机这条连接的指纹取不到(TOFU 时选了「只信任这一次」,没写
        // known_hosts)—— 宁可慢,不许猜。
        assert_eq!(route_for("SHA256:abc", None), Route::Slow);
    }

    /// F230:剪切在慢路径上**降级为复制**。跨机器的「移动」要在源端删,而
    /// 删除不可逆,且源端那条连接此刻可能已经断了(用户关掉了那个标签)——
    /// 删不掉又已经报了"已移动",用户会以为源没了。
    ///
    /// 自证会变红:把 `effective_mode` 里 Slow 那条分支去掉。
    #[test]
    fn cut_degrades_to_copy_off_the_fast_path() {
        assert_eq!(effective_mode(Route::Fast, ClipMode::Cut), ClipMode::Cut);
        assert_eq!(effective_mode(Route::Slow, ClipMode::Cut), ClipMode::Copy);
        assert_eq!(effective_mode(Route::Slow, ClipMode::Copy), ClipMode::Copy);
    }
}
```

- [ ] **Step 2：跑一次确认它红**

```bash
cargo test -p mullion-app --lib clipboard_remote 2>&1 | tail -20
```

- [ ] **Step 3：实现**

在同文件里，测试模块**之前**写：

```rust
//! F230:跨进程远端剪贴板的**载荷格式与路由判据**。零平台代码、零 IO。
//!
//! Windows 的 OLE 部分在 `dragout::win`。放在这里的是两件在无头环境验得了、
//! 而且错了最致命的事:
//!
//! 1. **载荷的字节编码** —— 远端文件名不保证是 UTF-8(`RemotePath` 整个
//!    类型就是为它存在的),走一趟 `String` 会把名字换成替换字符,粘贴时
//!    打不中那个文件。
//! 2. **快路径的准入判据** —— 指纹对不上却走了快路径,`copy_tree` 会在
//!    **本机**上找同名路径(`/srv/app` 这种路径两台机器上都有),静默复制
//!    错文件。这是这一片最危险的失误,所以判据必须是值级可测的纯函数。

use crate::files::clip::{ClipMode, RemoteClip};
use mullion_ssh::sftp::RemotePath;

/// 私有剪贴板格式的名字。用 `RegisterClipboardFormatW` 登记 —— 名字相同的
/// 两个进程拿到同一个 id,这正是"两个 Mullion 互通"要的。
pub const FORMAT_NAME: &str = "MullionRemoteFiles";

/// 载荷魔数 + 版本。换格式时改这里,旧载荷自然解不开(`decode` 返回 `None`,
/// 调用方退回慢路径)—— 比"尽力解析旧版"安全:半份垃圾会变成一批发给远端
/// 的乱码路径。
const MAGIC: &[u8; 8] = b"MULCLIP1";

/// 解出来的一份跨进程剪贴板。
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct RemoteClipboard {
    /// 源端那台机器的主机密钥指纹(`SHA256:<base64>`,与 `known_hosts` 同格式)。
    pub fingerprint: String,
    pub clip: RemoteClip,
}

/// 这次粘贴走哪条路。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Route {
    /// 同一台机器 —— 远端直接 `copy_tree`/`rename`,零传输。
    Fast,
    /// 不同机器(或本机指纹取不到)—— 先下载再上传。
    Slow,
}

/// 编成字节。**长度前缀的二进制**,不是文本:路径是裸字节,任何以行分隔的
/// 文本格式都会被名字里的 `\n` 撑破(远端文件名里放得下换行)。
///
/// 布局:`MAGIC(8) | mode(1) | fp_len(u16le) | fp | count(u32le) |`
/// 每项 `is_dir(1) | path_len(u32le) | path`。
pub fn encode(fingerprint: &str, clip: &RemoteClip) -> Vec<u8> {
    let mut out = Vec::new();
    out.extend_from_slice(MAGIC);
    out.push(match clip.mode {
        ClipMode::Copy => 0,
        ClipMode::Cut => 1,
    });
    let fp = fingerprint.as_bytes();
    out.extend_from_slice(&(fp.len() as u16).to_le_bytes());
    out.extend_from_slice(fp);
    out.extend_from_slice(&(clip.items.len() as u32).to_le_bytes());
    for (p, is_dir) in &clip.items {
        out.push(u8::from(*is_dir));
        let b = p.as_bytes();
        out.extend_from_slice(&(b.len() as u32).to_le_bytes());
        out.extend_from_slice(b);
    }
    out
}

/// 解回来。**任何一处对不上就整份作废**(返回 `None`),不做尽力解析 ——
/// 半份垃圾会变成一批发给远端的乱码路径。
pub fn decode(bytes: &[u8]) -> Option<RemoteClipboard> {
    let mut at = 0usize;
    let take = |at: &mut usize, n: usize| -> Option<&[u8]> {
        let end = at.checked_add(n)?;
        let s = bytes.get(*at..end)?;
        *at = end;
        Some(s)
    };
    if take(&mut at, 8)? != MAGIC {
        return None;
    }
    let mode = match take(&mut at, 1)?[0] {
        0 => ClipMode::Copy,
        1 => ClipMode::Cut,
        _ => return None,
    };
    let fp_len = u16::from_le_bytes(take(&mut at, 2)?.try_into().ok()?) as usize;
    let fingerprint = std::str::from_utf8(take(&mut at, fp_len)?).ok()?.to_owned();
    let count = u32::from_le_bytes(take(&mut at, 4)?.try_into().ok()?) as usize;
    let mut items = Vec::with_capacity(count.min(4096));
    for _ in 0..count {
        let is_dir = match take(&mut at, 1)?[0] {
            0 => false,
            1 => true,
            _ => return None,
        };
        let len = u32::from_le_bytes(take(&mut at, 4)?.try_into().ok()?) as usize;
        items.push((RemotePath::from_bytes(take(&mut at, len)?.to_vec()), is_dir));
    }
    // 末尾多出来的字节同样作废:那说明格式对不上,不是"多带了点东西"。
    if at != bytes.len() {
        return None;
    }
    Some(RemoteClipboard {
        fingerprint,
        clip: RemoteClip { mode, items },
    })
}

/// 这次粘贴该走哪条路。`here` = 本标签当前连接那台机器在 `known_hosts` 里的
/// 指纹;`None` = 取不到(TOFU 时用户选了「只信任这一次」,没写盘)。
///
/// **取不到就走慢路径,不许猜**:猜错的后果是在本机上按绝对路径复制 ——
/// `/srv/app` 这种路径两台机器上都存在,复制的是**另一台机器的同名文件**,
/// 而且完全静默。
pub fn route_for(payload: &str, here: Option<&str>) -> Route {
    match here {
        Some(h) if h == payload => Route::Fast,
        _ => Route::Slow,
    }
}

/// 慢路径上「剪切」降级成「复制」。
///
/// 跨机器的移动要在**源端**删,而删除不可逆,且源端那条连接此刻可能已经断了
/// (用户关掉了那个标签)。删不掉又已经报了"已移动",用户会以为源没了。
/// 调用方必须把降级**说出来**(toast),不能静默 —— 用户按的是剪切。
pub fn effective_mode(route: Route, mode: ClipMode) -> ClipMode {
    match route {
        Route::Fast => mode,
        Route::Slow => ClipMode::Copy,
    }
}
```

在 `crates/mullion-app/src/lib.rs` 里加 `pub mod clipboard_remote;`（按文件里现有的模块声明顺序插入）。

- [ ] **Step 4：跑绿并提交**

```bash
cargo test -p mullion-app --lib clipboard_remote 2>&1 | tail -5
cargo clippy -p mullion-app --all-targets -- -D warnings 2>&1 | tail -5
git add crates/mullion-app/src/clipboard_remote.rs crates/mullion-app/src/lib.rs
git commit -m "feat(app): 跨进程远端剪贴板的载荷编解码与快路径判据 (F230)

长度前缀的二进制格式 —— 远端文件名不保证 UTF-8,也放得下换行,任何文本
格式都会被撑破。指纹对不上一律走慢路径:猜错等于在本机按绝对路径复制,
静默复制到另一台机器的同名文件。

守护:clipboard_remote::tests::a_payload_round_trips_including_non_utf8_names
      clipboard_remote::tests::only_a_matching_fingerprint_takes_the_fast_path
      clipboard_remote::tests::cut_degrades_to_copy_off_the_fast_path

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 7.2：描述符支持相对路径（目录展开）

**Files:**
- Modify: `crates/mullion-app/src/dragout/descriptor.rs`

**为什么**：目录要以「一批带相对路径的文件」交给资源管理器（`sub\dir\file.txt`）。`FILEDESCRIPTORW.cFileName` 本来就允许相对路径，`FD_ATTRIBUTES` 那一位也要按项给（目录项要 `FILE_ATTRIBUTE_DIRECTORY`，否则空目录不会被建出来）。

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/dragout/descriptor.rs` 的 `mod tests`：

```rust
    /// F230:目录展开之后,项的名字是**反斜杠分隔的相对路径**。写成正斜杠
    /// 的话资源管理器会把整串当成一个文件名(Windows 上 `/` 是合法文件名
    /// 字符吗?不是 —— 它会被 `sanitize` 换成 `_`),结果是一堆
    /// `sub_dir_file.txt` 平铺在目标目录里,目录结构整个丢掉。
    ///
    /// 自证会变红:把 `Described::name` 里的分隔符换回 `/`。
    #[test]
    fn a_nested_item_keeps_its_relative_path_with_backslashes() {
        let items = [Described {
            name: r"sub\dir\file.txt",
            size: 7,
            is_dir: false,
        }];
        let buf = file_group_descriptor(&items);
        assert_eq!(read_name(&buf, 0), r"sub\dir\file.txt");
    }

    /// F230:目录项要带 `FILE_ATTRIBUTE_DIRECTORY`,否则空目录不会被建出来
    /// (资源管理器只按"有内容的文件"建父目录)—— 用户拖一棵树过去,
    /// 空子目录静默消失。
    ///
    /// 自证会变红:让 `write_one` 恒写 `FILE_ATTRIBUTE_NORMAL`。
    #[test]
    fn directory_entries_carry_the_directory_attribute() {
        let items = [
            Described { name: "empty", size: 0, is_dir: true },
            Described { name: "a.txt", size: 3, is_dir: false },
        ];
        let buf = file_group_descriptor(&items);
        assert_eq!(read_attrs(&buf, 0), FILE_ATTRIBUTE_DIRECTORY);
        assert_eq!(read_attrs(&buf, 1), FILE_ATTRIBUTE_NORMAL);
    }
```

在同 `mod tests` 里加两个读回辅助（如果文件里已经有等价的，照抄它们，不要重复造）：

```rust
    fn read_attrs(buf: &[u8], i: usize) -> u32 {
        let base = 4 + FD_SIZE * i;
        u32::from_le_bytes(buf[base + 36..base + 40].try_into().unwrap())
    }

    fn read_name(buf: &[u8], i: usize) -> String {
        let base = 4 + FD_SIZE * i + FD_NAME_OFF;
        let mut u = Vec::new();
        for k in 0..FD_NAME_CAP {
            let c = u16::from_le_bytes(buf[base + k * 2..base + k * 2 + 2].try_into().unwrap());
            if c == 0 {
                break;
            }
            u.push(c);
        }
        String::from_utf16(&u).unwrap()
    }
```

- [ ] **Step 2：跑一次确认它红，然后实现**

`Described` 加 `pub is_dir: bool`；`FILE_ATTRIBUTE_DIRECTORY` 常量（`0x0000_0010`）改成 `pub const`（测试要读）；`write_one` 里属性那一行改成：

```rust
    // F230:目录项必须带 `FILE_ATTRIBUTE_DIRECTORY` —— 否则空子目录不会被
    // 建出来,用户拖一棵树过去,空目录静默消失。
    let attrs = if it.is_dir {
        FILE_ATTRIBUTE_DIRECTORY
    } else {
        FILE_ATTRIBUTE_NORMAL
    };
    fd[36..40].copy_from_slice(&attrs.to_le_bytes());
```

同时把 `FD_FLAGS` 那条注释里「目录不拖(设计 N2)」的说法改掉——F230 之后目录会拖。

`dragout/mod.rs::items_for` 的 `Described` 构造点补 `is_dir: false`（拖出仍然跳过目录，N2 的理由在拖拽场景下依然成立：起拖那一刻不能卡几十秒去递归列目录）。

- [ ] **Step 3：跑绿并提交**

```bash
cargo test -p mullion-app --lib dragout 2>&1 | tail -5
git add crates/mullion-app/src/dragout/
git commit -m "feat(app): 文件描述符支持相对路径与目录项属性 (F230)

守护:dragout::descriptor::tests::a_nested_item_keeps_its_relative_path_with_backslashes
      dragout::descriptor::tests::directory_entries_carry_the_directory_attribute

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 7.3：常驻 STA 线程 + `OleSetClipboard`（Windows）

**Files:**
- Modify: `crates/mullion-app/src/dragout/win.rs`

**为什么要常驻线程**：`OleSetClipboard` 要求调用线程是 STA，**并且**延迟渲染的 `GetData` 回调会回到**那条线程**。F59 的一次性拖出线程在 `DoDragDrop` 返回后就退出了——用那种线程放剪贴板，用户按 Ctrl+V 时回调没人接，粘出来是空的（而且完全静默）。

> **本机验证不了**：这一整段只有交叉编译到 Windows、在真机上才能验。写完照 `.claude/skills/release-windows/SKILL.md` 出 exe，把验收项写进 PR，**不要**声称「已完成」。

- [ ] **Step 1：实现常驻线程**

在 `crates/mullion-app/src/dragout/win.rs` 末尾加：

```rust
// -------------------------------------------------- F230:剪贴板(常驻 STA)

/// 发给常驻 STA 线程的活。
enum ClipJob {
    /// 把这一批远端文件放进系统剪贴板。
    Set {
        payload: Vec<u8>,
        items: Vec<DragOutItem>,
        runtime: tokio::runtime::Handle,
        sftp: Arc<SftpClient>,
    },
}

/// 常驻 STA 线程的投递口。**必须常驻**:`OleSetClipboard` 之后,延迟渲染的
/// `GetData` 回调会回到**放剪贴板的那条线程**。F59 那种一次性线程在
/// `DoDragDrop` 返回后就退出了 —— 用它放剪贴板,用户按 Ctrl+V 时没人接
/// 回调,粘出来是空的,而且完全静默(我们这边一条日志都不会有,失败发生
/// 在别人的进程里)。
///
/// 线程里跑一个消息泵:OLE 的 STA 契约要求线程能处理窗口消息,否则跨线程
/// 的 marshalling 会死等。
static CLIP_TX: std::sync::OnceLock<std::sync::mpsc::Sender<ClipJob>> = std::sync::OnceLock::new();

/// 取(必要时启动)常驻 STA 线程的投递口。
fn clip_tx() -> &'static std::sync::mpsc::Sender<ClipJob> {
    CLIP_TX.get_or_init(|| {
        let (tx, rx) = std::sync::mpsc::channel::<ClipJob>();
        let spawned = std::thread::Builder::new()
            .name("mullion-clipboard".into())
            .spawn(move || clip_thread(&rx));
        if let Err(e) = spawned {
            log::error!(target: LOG, "起剪贴板 STA 线程失败:{e}");
        }
        tx
    })
}

fn clip_thread(rx: &std::sync::mpsc::Receiver<ClipJob>) {
    if let Err(e) = unsafe { OleInitialize(None) } {
        log::error!(target: LOG, "剪贴板线程 OleInitialize 失败:{e}");
        return;
    }
    // **不 `OleUninitialize`**:这条线程活到进程结束。提前 uninit 会让已经
    // 放进剪贴板的 `IDataObject` 失效,用户按 Ctrl+V 粘出来是空的。
    loop {
        // 阻塞等活,但要在等的同时抽消息泵 —— STA 线程不抽泵,跨线程
        // marshalling 会死等(而剪贴板的 `GetData` 正是跨进程调过来的)。
        match rx.recv_timeout(std::time::Duration::from_millis(50)) {
            Ok(ClipJob::Set { payload, items, runtime, sftp }) => {
                set_clipboard(payload, items, runtime, sftp);
            }
            Err(std::sync::mpsc::RecvTimeoutError::Timeout) => {}
            Err(std::sync::mpsc::RecvTimeoutError::Disconnected) => break,
        }
        pump_messages();
    }
}

/// 抽干这条线程当前排队的窗口消息。
fn pump_messages() {
    use windows::Win32::UI::WindowsAndMessaging::{
        DispatchMessageW, PeekMessageW, TranslateMessage, MSG, PM_REMOVE,
    };
    let mut msg = MSG::default();
    while unsafe { PeekMessageW(&mut msg, None, 0, 0, PM_REMOVE) }.as_bool() {
        unsafe {
            let _ = TranslateMessage(&msg);
            DispatchMessageW(&msg);
        }
    }
}

fn set_clipboard(
    payload: Vec<u8>,
    items: Vec<DragOutItem>,
    runtime: tokio::runtime::Handle,
    sftp: Arc<SftpClient>,
) {
    use windows::Win32::System::Ole::OleSetClipboard;
    let formats = Formats::register();
    let described: Vec<descriptor::Described<'_>> = items
        .iter()
        .map(|i| descriptor::Described {
            name: &i.name,
            size: i.size,
            is_dir: false,
        })
        .collect();
    let bytes = descriptor::file_group_descriptor(&described);
    let data: IDataObject = Items {
        formats,
        descriptor: bytes,
        items,
        runtime,
        sftp,
        // F230:私有格式的载荷。`Items` 要新增这两个字段
        // (`private_format: u16` / `private_payload: Vec<u8>`),
        // 并在 `GetData`/`QueryGetData`/`EnumFormatEtc` 里一并登记。
        private_format: register_format(crate::clipboard_remote::FORMAT_NAME),
        private_payload: payload,
    }
    .into();
    match unsafe { OleSetClipboard(&data) } {
        Ok(()) => log::info!(target: LOG, "已放入系统剪贴板"),
        Err(e) => log::error!(target: LOG, "OleSetClipboard 失败:{e}"),
    }
    // **不调 `OleFlushClipboard`**:那会把所有延迟渲染的内容立刻拉下来 ——
    // 一棵几百 MB 的远端目录树会当场下载完。代价是「Mullion 一关剪贴板就
    // 失效」,这是与用户确认过、明确接受的取舍(F230)。
}

/// 把一批远端文件放进系统剪贴板。立刻返回 —— 真正的活在常驻 STA 线程里。
pub fn set(
    runtime: tokio::runtime::Handle,
    sftp: Arc<SftpClient>,
    items: Vec<DragOutItem>,
    payload: Vec<u8>,
) {
    if let Err(e) = clip_tx().send(ClipJob::Set { payload, items, runtime, sftp }) {
        log::error!(target: LOG, "投递剪贴板任务失败:{e}");
    }
}

/// 读系统剪贴板里的 Mullion 私有载荷。没有就 `None`(调用方接着看
/// `CF_HDROP`,那是用户在资源管理器里复制的本地文件)。
pub fn read_private() -> Option<Vec<u8>> {
    // 用 `OleGetClipboard` + `GetData`,不用 `GetClipboardData` ——
    // 后者要求先 `OpenClipboard`,而 OLE 放进去的对象要走 OLE 那条路才
    // 能触发源进程的延迟渲染回调。
    // …具体实现见 `Items::GetData` 的镜像写法…
    todo!("按 win.rs 里既有的 FORMATETC/STGMEDIUM 用法实现")
}

/// 读系统剪贴板里的 `CF_HDROP`(用户在资源管理器里 Ctrl+C 的本地文件)。
pub fn read_hdrop() -> Vec<std::path::PathBuf> {
    // …同上,复用 F52 已经有的 `DroppedFile` 路径解析…
    todo!("按 win.rs 里既有的写法实现")
}
```

> 上面两个 `todo!()` **必须在本任务内写完**——它们是 F230 的粘贴侧，留着就等于功能只做了一半。实现时**照抄 `Items::GetData` 里已有的 `FORMATETC`/`STGMEDIUM` 用法**（那段已经在真机上验过），不要另找一套 API。`windows-rs 0.59` 的签名以本地 `cargo doc -p windows --open` 或 `~/.cargo/registry/src/**/windows-*/` 里的实际声明为准，**不要凭记忆写**（本仓库为 windows-rs 0.59 的签名踩过三个坑，见 `slice-d4b-dragout-progress` 那条记忆）。

- [ ] **Step 2：`Items` 加私有格式**

`Items` 结构体加 `private_format: u16` / `private_payload: Vec<u8>` 两个字段，并在 `QueryGetData` / `GetData` / `EnumFormatEtc` 三处**一并**登记。

> **三处必须一起改**：只加 `GetData` 的话，别的程序先问 `QueryGetData` 得到「没有这个格式」就不会再来取——粘不出来且完全静默。本仓库把这类叫「列举式门控在加档时必然漏」，已经踩中四次。

- [ ] **Step 3：编译检查（交叉编译）**

```bash
cargo check --target x86_64-pc-windows-gnu -p mullion-app 2>&1 | tail -20
```
预期：无错误。**本机跑不了这段代码**，只能验编译。

- [ ] **Step 4：提交**

```bash
git add crates/mullion-app/src/dragout/win.rs
git commit -m "feat(app): 常驻 STA 线程 + OleSetClipboard,私有格式与虚拟文件同时上板 (F230)

常驻是硬要求:延迟渲染的 GetData 回调会回到放剪贴板的那条线程,一次性
线程退出后没人接回调,粘出来是空的且完全静默。不调 OleFlushClipboard
(会把整棵目录树当场下载完),代价是关掉 Mullion 剪贴板即失效。

未验证:OLE 行为只能在 Windows 真机上验,已列入人工验收清单。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 7.4：Ctrl+C / Ctrl+X / Ctrl+V 接到系统剪贴板

**Files:**
- Modify: `crates/mullion-app/src/app.rs`

- [ ] **Step 1：写失败的测试**

加到 `crates/mullion-app/src/app.rs` 的 `mod tests`：

```rust
    /// F230:粘贴必须**先看系统剪贴板**,进程内那份 `files.clip` 只是显示用
    /// (画淡剪切源)。两份真值并存的话,用户在别的 Mullion 里 Ctrl+C 之后回来
    /// 粘,粘出来的是自己上次复制的旧东西 —— 而且看不出哪里不对。
    ///
    /// 自证会变红:把 `start_paste` 改回先读 `files.clip`。
    #[test]
    fn pasting_consults_the_system_clipboard_before_the_in_process_one() {
        let src = include_str!("app.rs");
        let anchor = "fn start_paste(&mut self, generation: u64)";
        let at = src.find(anchor).expect("找不到 start_paste");
        let seg = &src[at..at + 1200];
        let sys = seg.find("clipboard_remote::decode").or(seg.find("read_private"));
        let local = seg.find("files.clip");
        match (sys, local) {
            (Some(s), Some(l)) => assert!(s < l, "系统剪贴板必须排在 files.clip 之前(F230)"),
            (Some(_), None) => {}
            _ => panic!("start_paste 没有读系统剪贴板 —— F230 的跨进程粘贴不成立"),
        }
    }

    /// F230:剪切降级成复制时**必须说出来**。用户按的是剪切,源还在,不说
    /// 一句的话他会以为源已经没了(而实际上两边各留了一份)。
    ///
    /// 自证会变红:把降级分支里的 `set_toast` 删掉。
    #[test]
    fn a_degraded_cut_tells_the_user_why_the_source_is_still_there() {
        let src = include_str!("app.rs");
        let anchor = "clipboard_remote::effective_mode(";
        let at = src.find(anchor).expect("找不到降级判据的调用点");
        let seg = &src[at..at + 800];
        assert!(
            seg.contains("set_toast"),
            "剪切降级成复制没有任何提示 —— 用户会以为源已经被移走了"
        );
    }
```

- [ ] **Step 2：Ctrl+C / Ctrl+X 写系统剪贴板**

在 `FileAction::ClipCopy` / `ClipCut` 的处理里，**保留**既有的 `files.clip = Some(clip.clone())`（它负责画淡剪切源），在其后加：

```rust
                // F230:同一份内容也放进系统剪贴板 —— 另一个 Mullion 实例、
                // 资源管理器、别的程序都靠它。`files.clip` 只剩"画淡剪切源"
                // 这一个用途,粘贴一律以系统剪贴板为准。
                #[cfg(windows)]
                {
                    // 指纹取自 known_hosts 里这台机器那一条 —— `check_server_key`
                    // 过了就意味着「实测指纹 == 记录指纹」,两者等价。取不到
                    // (TOFU 时选了「只信任这一次」)就给空串,对面必然判成
                    // 不匹配 → 慢路径,安全。
                    let fp = self.host_fingerprint_for(generation).unwrap_or_default();
                    let payload = crate::clipboard_remote::encode(&fp, &clip);
                    if let (Some(sftp), Some(rt)) = (sftp_client, Some(self._runtime.handle().clone())) {
                        crate::dragout::win::set(rt, sftp, dragout_items, payload);
                    }
                }
```

并新增一个取指纹的小方法（放在 `App` 的 impl 里，靠近别的 `*_for(generation)` 辅助）：

```rust
    /// F230:这个标签当前连的那台机器在 `known_hosts` 里的指纹。
    ///
    /// **不从 `SshConnection` 上取**:`check_server_key` 算过指纹但没存下来,
    /// 而给 `mullion-ssh` 加一个字段是跨 crate 改动,为一个纯 app 侧的特性
    /// 不值当(Scope Discipline)。两者等价:`check_server_key` 过了就意味着
    /// 「实测指纹 == 记录指纹」。
    ///
    /// 取不到(TOFU 时用户选了「只信任这一次」,没写盘)返回 `None` ——
    /// 调用方据此走慢路径,不猜。
    fn host_fingerprint_for(&self, generation: u64) -> Option<String> {
        let tab = self.tabs.by_generation(generation)?;
        let (host, port) = tab.content.sftp_endpoint()?;
        let id = mullion_ssh::known_hosts::host_key_id(&host, port);
        let known = self.known_hosts.lock().ok()?;
        Some(known.get(&id)?.fingerprint.clone())
    }
```

> `sftp_endpoint()` 的实际名字以 `TabContent` 现有 API 为准（`sftp_connection_for` / `sftp_host_ix` 附近应该已经能拿到 host/port）——**照抄那里的取法**，不要另写一遍解析。

- [ ] **Step 3：Ctrl+V 分三路**

`start_paste` 改成：

```rust
    fn start_paste(&mut self, generation: u64) {
        // F230:**系统剪贴板是唯一真值**。`files.clip` 只负责画淡剪切源 ——
        // 两份真值并存的话,用户在另一个 Mullion 里 Ctrl+C 之后回来粘,
        // 粘出来的是自己上次复制的旧东西,而且看不出哪里不对。
        #[cfg(windows)]
        if let Some(bytes) = crate::dragout::win::read_private() {
            if let Some(rc) = crate::clipboard_remote::decode(&bytes) {
                let here = self.host_fingerprint_for(generation);
                let route = crate::clipboard_remote::route_for(&rc.fingerprint, here.as_deref());
                let mode = crate::clipboard_remote::effective_mode(route, rc.clip.mode);
                if mode != rc.clip.mode {
                    self.ui.set_toast(
                        crate::ui::toast::Kind::Warn,
                        "跨机器粘贴按复制处理:源端文件保留,请确认无误后手动删除",
                    );
                }
                let dst = /* 面板当前目录,取法同下 */;
                match route {
                    crate::clipboard_remote::Route::Fast => {
                        let clip = crate::files::clip::RemoteClip { mode, items: rc.clip.items };
                        self.start_paste_of(generation, clip, dst);
                    }
                    // 慢路径:先下载到临时目录再上传。复用 F52 的传输队列 ——
                    // 它已经有进度、冲突处置、取消。
                    crate::clipboard_remote::Route::Slow => {
                        self.start_cross_host_paste(generation, rc.clip.items, dst);
                    }
                }
                return;
            }
        }
        // 只有 CF_HDROP —— 用户在资源管理器里 Ctrl+C 的本地文件。走 F52 那条
        // 上传通路(`group_by_parent` + `plan_transfer`),一个字都不用改。
        #[cfg(windows)]
        {
            let paths = crate::dragout::win::read_hdrop();
            if !paths.is_empty() {
                self.start_upload_of(generation, &paths);
                return;
            }
        }
        // 非 Windows / 系统剪贴板里没有我们认识的东西:退回 F220 的进程内行为。
        // …这里放原来那段读 `files.clip` + `files.remote.cwd` 的代码…
    }
```

`start_cross_host_paste` 与 `start_upload_of` 两个新方法：前者把远端项下载到 `std::env::temp_dir()` 下的一个专用子目录再上传（复用既有的 `plan_transfer` / 传输队列）；后者就是 F52 拖入那条路的**函数化**（把 `HoveredFile`/`DroppedFile` 那段里 `group_by_parent` 之后的部分抽成方法，两个调用点共用——**不要复制一份**）。

- [ ] **Step 4：跑绿 + 交叉编译检查 + clippy**

```bash
cargo test --workspace > /tmp/t.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/t.log
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
cargo check --target x86_64-pc-windows-gnu -p mullion-app 2>&1 | tail -10
```

- [ ] **Step 5：提交，然后做本 Part 全部 7 条 doc comment 里写明的变异验证**

```bash
git add crates/mullion-app/src
git commit -m "feat(app): SFTP 文件可在两个 Mullion 之间复制粘贴 (F230)

系统剪贴板成为唯一真值(files.clip 只剩画淡剪切源)。Ctrl+C/X 同时写
Mullion 私有格式(带主机密钥指纹)与虚拟文件格式。Ctrl+V 三路分流:
指纹一致走远端 copy_tree 零传输;不一致下载再上传且剪切降级为复制并
明确告知;只有 CF_HDROP 时按 F52 上传。

未验证:OLE 剪贴板与真实资源管理器/另一个实例的互操作只能在 Windows
真机上验,已列入人工验收清单。

守护:app::tests::pasting_consults_the_system_clipboard_before_the_in_process_one
      app::tests::a_degraded_cut_tells_the_user_why_the_source_is_still_there

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Part 8 — 收尾与发版

### Task 8.1：整体跑绿 + 变异验证复盘

- [ ] **Step 1：全量绿**

```bash
cargo test --workspace > /tmp/test.log 2>&1; grep -nE "test result|FAILED|panicked" /tmp/test.log
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
cargo fmt --check
```
预期：`test result: ok` 全部为 ok，clippy 无输出，fmt 无输出。**只跑单个 crate 不叫绿。**

- [ ] **Step 2：把本计划里每条 doc comment 写的「自证会变红」真跑一遍**

清单（每条都要**先 `git commit`**再改——本仓库已有 5 次未提交编辑被 `git checkout` 吞掉）：

| # | 测试 | 变异 |
|---|---|---|
| 1 | `ui::tests::starting_a_probe_makes_room_for_its_own_result` | `begin_probe` 里删掉 `self.error_dismissed = true;` |
| 2 | `ui::tests::a_probe_does_not_erase_the_error_it_stepped_over` | `begin_probe` 里改成 `self.last_error = None;` |
| 3 | `preset::tests::closing_a_pane_lands_on_the_preset_that_shape_now_matches` | `same_shape` 里把 `ratio` 比较去掉 |
| 4 | `preset::tests::a_shape_that_matches_no_preset_reports_none` | `preset_of` 末尾 fallback 成 `Some(Preset::Single)` |
| 5 | `app::tests::the_highlighted_preset_is_derived_from_the_tree_not_remembered` | 把 `preset:` 那行改回读一个字段 |
| 6 | `local::tests::a_configured_path_wins_over_the_bookmark` | `pick_default_local` 里把收藏那条提到 configured 之前 |
| 7 | `local::tests::a_bookmark_that_no_longer_exists_is_skipped` | 去掉 `bookmark_exists` 那个判据 |
| 8 | `state::tests::starting_a_new_dir_cancels_a_rename_in_flight` | `begin_new` 里删掉 `self.rename_edit = None;` |
| 9 | `files_panel::tests::the_ghost_row_says_folder_when_that_is_what_we_are_making` | 幽灵行图标/占位串写死成文件那套 |
| 10 | `app::tests::a_new_folder_is_revealed_after_it_lands` | `follow` 里去掉 `FileOp::NewDir(p)` 那一支 |
| 11 | `app::tests::ctrl_shift_n_makes_a_folder_not_a_file` | 键位判断里去掉 `mods.shift_key()` 分支 |
| 12 | `vault::tests::a_clone_carries_the_credentials_over` | `clone_session` 里不复制 `SecretEntry` |
| 13 | `vault::tests::a_clone_gets_a_free_name` | `clone_name` 恒返回 `base.to_owned()` |
| 14 | `vault::tests::every_mutating_entry_point_reloads_before_it_writes` | `clone_session` 首行删掉 `sync_from_disk_if_untouched()` |
| 15 | `list::tests::right_click_offers_clone` | 菜单里删掉「克隆」项 |
| 16 | `drag::tests::a_same_column_drop_onto_the_current_dir_is_refused` | `drop_intent` 里去掉 `from == onto && over_dir.is_none()` 那条 |
| 17 | `drag::tests::ctrl_turns_a_within_column_move_into_a_copy` | 忽略 `ctrl` 参数 |
| 18 | `app::tests::a_within_column_drop_does_not_clobber_the_users_clipboard` | 把合成的 clip 写进 `self.files.clip` |
| 19 | `clipboard_remote::tests::a_payload_round_trips_including_non_utf8_names` | `encode` 里改用 `p.display()` 的字节 |
| 20 | `clipboard_remote::tests::only_a_matching_fingerprint_takes_the_fast_path` | `route_for` 的条件反过来 |
| 21 | `clipboard_remote::tests::cut_degrades_to_copy_off_the_fast_path` | `effective_mode` 去掉 Slow 分支 |
| 22 | `descriptor::tests::a_nested_item_keeps_its_relative_path_with_backslashes` | 分隔符换回 `/` |
| 23 | `descriptor::tests::directory_entries_carry_the_directory_attribute` | `write_one` 恒写 `FILE_ATTRIBUTE_NORMAL` |
| 24 | `app::tests::pasting_consults_the_system_clipboard_before_the_in_process_one` | `start_paste` 改回先读 `files.clip` |
| 25 | `app::tests::a_degraded_cut_tells_the_user_why_the_source_is_still_there` | 删掉降级分支里的 `set_toast` |

每条：改 → 跑对应测试 → **确认它红** → `git checkout -- <file>` 还原。
**没变红的那条要当场修测试**，不是记一笔了事——恒绿的守护测试比没有守护更糟（它给人"有人看着"的错觉）。

> **源码切片守护的已知盲区**（本仓库踩过五次）：`include_str!` 的断言会被**注释里的同名关键词**误伤或误绿。写这类断言时，锚点要用「紧邻上一行」的结构位置，别用裸前缀；变异时优先改**运算符方向**而不是删字符串（删字符串会被别处的注释接住）。

- [ ] **Step 3：确认没有半截活**

```bash
grep -rn "todo!\|unimplemented!\|TODO(F2[23]" crates/ --include=*.rs
```
预期：**无输出**。Task 7.3 里那两个 `todo!()` 是占位说明，实施时必须写完；这里查一遍确保没漏。

### Task 8.2：发版

- [ ] **Step 1：按 release skill 一条龙做完**

本轮改动落到了 `mullion-app`，按 `CLAUDE.md` 的交付约定**默认执行、不再问**：
升 patch（`0.1.96` → `0.1.97`）→ 跑绿 → 交叉编译 + objdump 验收 → 签名 → 发 GitHub Release。

完整步骤读 `.claude/skills/release-windows/SKILL.md`，**别凭记忆做**——每一步都有漏了也不报错的坑。
`gh`/`curl` 全部走 socks 代理（本机 DNS 解析不了 github）；push 要显式带 `nc socks5` 的 `ProxyCommand`。
私密信息（真机 IP / 用户名 / 私钥路径 / 凭据）**永不进被跟踪文件、永不推送**。

- [ ] **Step 2：Release notes 里附这份人工验收清单**

无头容器里验不了的，一条都别说「已完成」：

**F226 就地新建文件夹**
1. 远端栏右键空白处 →「新建文件夹…」→ 列表里就地出现一行带文件夹图标的输入行，光标在里面。
2. 打中文名，回车 → 文件夹建出来、列表刷新、**光标落在新建的那一行**上。
3. 输入过程中按 Esc → 幽灵行消失，什么也没建。
4. `Ctrl+Shift+N` 等价于菜单项；`Ctrl+N` 仍是新建文件。
5. 重名时红字提示，回车不提交。

**F227 远端栏内拖拽**
6. 远端栏里选中 3 个文件，拖到同栏的某个目录上 → 目录高亮，胶囊写「移动 3 项到 <目录名>」，松手后文件移过去。
7. 按住 Ctrl 再拖 → 胶囊改成「复制」，松手后源还在。
8. 拖到栏内空白处（不在任何目录上）→ **不接受**（没有落点高亮），松手什么也不发生。
9. 拖之前先 Ctrl+C 复制过别的东西 → 拖完再 Ctrl+V，粘出来的仍是**先前复制的那批**（栏内拖拽不许污染剪贴板）。

**F228 拨测卡片**
10. 先制造一次失败（填个不存在的主机，保存）→ 顶部留着红错误条 → 改回正确主机 → 点「测试连接」→ **立刻**能看到「正在测试连接…」，成功后显示绿结果。
11. 关掉拨测卡片后，先前那条错误**还在**（没被顺手清掉）。

**F229 会话克隆**
12. 会话列表里右键任一条 →「克隆」→ 新出一条 `<原名> 副本`，右栏自动切到它、名字框获得焦点并全选。
13. 克隆一条**用密码登录**的会话 → 直接点连接，**不用重填密码**（凭据跟着过来了）。
14. 克隆一条引用**共享凭据**（F74）的会话 → 仍然是引用，不是复制一份。
15. 克隆一条带跳板/隧道/登录后自动化的会话 → 三样都在。

**F230 跨进程剪贴板**（风险最高，重点验）
16. 开两个 Mullion，**连同一台机器**：A 里 Ctrl+C 几个文件 → B 里 Ctrl+V → **秒完成**（走远端 copy，没有传输进度条）。
17. 两个 Mullion **连不同机器**：A 里 Ctrl+C → B 里 Ctrl+V → 有传输进度条，完成后文件正确。
18. 同上，A 里按的是 **Ctrl+X** → B 粘完后 **A 的源文件还在**，并且出现一条说明「按复制处理」的提示。
19. Mullion 里 Ctrl+C 一个**目录** → 资源管理器里 Ctrl+V → 整棵树下来，**空子目录也在**。
20. 资源管理器里 Ctrl+C 几个本地文件 → Mullion 远端栏 Ctrl+V → 上传。
21. Mullion 里 Ctrl+C 之后**关掉 Mullion** → 资源管理器 Ctrl+V 失败（**这是已知且接受的行为**，不是 bug）。
22. 远端有非 UTF-8 文件名的文件 → 复制粘贴不报 `NoSuchFile`。

**F231 本地默认目录**
23. 本地栏收藏里有若干条 → 重开 Mullion，本地栏默认停在**收藏的第一条**。
24. 把收藏第一条对应的目录删掉再重开 → 本地栏落到下一条能打开的，或者家目录，不是空白/报错。

**F232 分屏预设高亮**
25. 选四宫格 → 菜单栏分屏区高亮在四宫格上 → 点标题条的关闭按钮关掉一个 pane → 高亮**不再是四宫格**（四宫格关掉任一个都不匹配任何预设，所以变成没有高亮）。
26. 选「左右两栏」→ 关掉一个 → 高亮变成「单屏」。
27. 选「左大右两小」→ 关掉左边那块大的 → 高亮变成「上下两栏」；改为关掉右边任一小块 → 高亮变成「左右两栏」。

**回归（每次发版都要过一遍）**
28. tmux 里跑 Claude Code，`/compact` 时不闪、划选高亮不被冲掉（T2'/T13）。
29. 中文输入法在终端里能打字，在 egui 输入框里打完中文回终端仍然能打（T10）。
30. UI 里没有豆腐块 `□`（T9）。

- [ ] **Step 3：把这一片写进记忆**

在 `/home/ubuntu/.claude/projects/-data-Mullion/memory/` 建
`slice-f226-f232-progress.md`（`type: project`），记本片**非显然**的东西：

- 「快路径的准入判据是主机密钥指纹」这条决定的理由——指纹对不上却走快路径，会在**本机**按绝对路径复制到同名文件，静默复制错文件。
- 「常驻 STA 线程」是硬要求：延迟渲染回调回到放剪贴板的那条线程，一次性线程退出后粘出来是空的且全程静默。
- 「`current_preset` 是影子状态，它唯一的更新规则本身就是那个 bug」——这是本仓库第 N 次踩「派生量被存成字段」。
- 「栏内拖拽路由进 F220 的粘贴流水线」而不是自己写移动：`drop_target` 原来的注释已经警告过同路径 `open_write` 截断会把文件清零。
- F229 走 store 层 `clone_session` 而不是 UI 层「草稿 + Save」的理由：后者会走到 `merge_secret(existing = None)`，**静默**产出一份没有凭据的克隆。

并在 `MEMORY.md` 追一行指针。

---

## Self-Review 记录

**1. 需求覆盖**：用户提的 7 条 ↔ Part：① 新建文件夹 → Part 4（F226）；② 远端栏内拖拽 → Part 6（F227）；③ 拨测结果被旧错误覆盖 → Part 1（F228）；④ 会话克隆 → Part 5（F229）；⑤ 跨 exe 复制粘贴 → Part 7（F230）；⑥ 本地栏默认收藏首条 → Part 3（F231）；⑦ 关分屏后菜单高亮 → Part 2（F232）。七条全有对应任务，无遗漏。

**2. 占位符扫描**：Task 7.3 里两个 `todo!()` 是**有意保留的实现指引**（windows-rs 0.59 的确切签名必须现查，不许凭记忆写），并已在该任务内用引用块明确「必须在本任务内写完」，且 Task 8.1 Step 3 用 `grep` 兜底。其余各步均给了完整代码与确切命令。

**3. 类型一致性**：跨任务复用的名字统一为——`preset_of` / `same_shape` / `RATIO_EPS`（F232）、`pick_default_local` / `default_local`（F231）、`NewKind` / `begin_new_dir` / `begin_new`（F226）、`clone_session` / `clone_name`（F229）、`DropKind` / `Within` / `drop_intent` / `preview_label` / `start_paste_of`（F227）、`clipboard_remote::{FORMAT_NAME, encode, decode, RemoteClipboard, Route, route_for, effective_mode}` / `dragout::win::{set, read_private, read_hdrop}` / `host_fingerprint_for` / `start_cross_host_paste` / `start_upload_of`（F230）、`UiState::{begin_probe, clone_request}`。各处签名前后一致。
